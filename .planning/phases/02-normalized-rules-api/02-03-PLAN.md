---
phase: 02-normalized-rules-api
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - srd_cli/combat_rules.py
  - tests/test_combat_rules.py
autonomous: true
requirements:
  - API-04
  - API-05
must_haves:
  truths:
    - "Caller can inspect each creature with ordered traits, actions, and structured attacks."
    - "Caller can inspect each weapon with resolved property objects and assignment detail."
    - "No creature attack or weapon property consumer must dereference raw foreign keys."
  artifacts:
    - path: "srd_cli/combat_rules.py"
      provides: "Combat-facing creature and weapon aggregate joins"
    - path: "tests/test_combat_rules.py"
      provides: "Creature/action/attack and weapon/property exhaustive tests"
  key_links:
    - from: "CreatureActionAttack.parent"
      to: "CreatureAction.pk"
      via: "nested structured attacks"
      pattern: "attacks"
    - from: "WeaponPropertyAssignment.property"
      to: "WeaponProperty.pk"
      via: "resolved assignment property"
      pattern: "property"
---

<objective>
Implement combat-facing normalized joins for creatures, actions/attacks/traits, weapons, and properties.

Purpose: Give Phase 4 structured legal combat content without raw-key traversal.
Output: Immutable creature/weapon aggregate APIs and exhaustive join tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-normalized-rules-api/02-CONTEXT.md
@.planning/phases/02-normalized-rules-api/02-01-SUMMARY.md
@srd_cli/data/srd521/Creature.json
@srd_cli/data/srd521/CreatureTrait.json
@srd_cli/data/srd521/CreatureAction.json
@srd_cli/data/srd521/CreatureActionAttack.json
@srd_cli/data/srd521/Weapon.json
@srd_cli/data/srd521/WeaponProperty.json
@srd_cli/data/srd521/WeaponPropertyAssignment.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Join creature traits, actions, and structured attacks</name>
  <files>srd_cli/combat_rules.py, tests/test_combat_rules.py</files>
  <behavior>
    - Creature lookup by exact normalized name/pk returns immutable aggregate.
    - Traits order by name/pk; actions order by order_in_statblock then name/pk.
    - Each action contains all attack rows ordered by name/pk.
    - Attack damage, extra damage, reach/range, target restriction, and to-hit fields retain source values.
    - Every trait/action/attack belongs to exactly one valid parent.
  </behavior>
  <action>Create frozen `CreatureView`, `CreatureActionView`, and `CombatRules` query service using Plan 01 models/indexes. Join Creature ← CreatureTrait and Creature ← CreatureAction ← CreatureActionAttack by exact keys per D-02. Keep action prose plus structured attack values; do not implement combat state or damage resolution (deferred). Apply deterministic order per D-04, using source `order_in_statblock` before names. Add exhaustive orphan, duplicate membership, count preservation, ambiguity, and immutability tests.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_combat_rules.py -x -k creature</automated>
  </verify>
  <done>All 331 creatures query successfully; all traits/actions/attacks resolve once with stable nested order and full structured attack data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Join weapons to resolved properties</name>
  <files>srd_cli/combat_rules.py, tests/test_combat_rules.py</files>
  <behavior>
    - Every weapon returns ordered property assignments.
    - Each assignment embeds resolved property identity/name/description/type plus assignment detail.
    - Every assignment resolves one weapon and one property.
    - Weapon damage and range fields retain typed source values.
  </behavior>
  <action>Add frozen weapon aggregate/property assignment views. Join `WeaponPropertyAssignment.weapon` and `.property` to typed entities; retain assignment `detail` (for values such as Versatile die) rather than flattening it away. Order properties by property type/name/pk. Reject orphan weapon/property keys. Keep source records read-only per D-01; do not add equipment inventory state.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_combat_rules.py -x</automated>
  </verify>
  <done>All 38 weapons and 108 assignments resolve deterministically; consumers receive property objects, not unresolved ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Creature relationship rows → combat view | Nested actions/attacks become future executable combat inputs |
| Weapon assignments → property view | Foreign keys determine weapon capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | creature joins | mitigate | Exhaustive exact-parent validation; preserve typed source attack fields; reject orphans |
| T-02-08 | T | weapon property joins | mitigate | Resolve both sides through unique pk indexes; preserve detail; reject missing refs |
| T-02-09 | D | nested aggregation | mitigate | Construct from finite indexed tuples, never recursively follow action references |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_combat_rules.py -x`
`py -3.14 -m srd_cli audit`
`py -3.14 -m ruff check srd_cli/combat_rules.py tests/test_combat_rules.py`
</verification>

<success_criteria>
- Creature aggregates expose every trait/action/attack with structured source values.
- Weapon aggregates resolve every property assignment and detail.
- Exhaustive tests prove no relationship row is dropped, duplicated, or orphaned.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalized-rules-api/02-03-SUMMARY.md`
</output>
