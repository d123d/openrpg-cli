---
phase: 02-normalized-rules-api
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - openrpg_cli/api.py
  - openrpg_cli/__init__.py
  - openrpg_cli/cli.py
  - tests/test_rules_api.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
must_haves:
  truths:
    - "Caller has one stable public read-only API for all character and combat aggregates."
    - "Existing browse/search/show/roll CLI behavior remains compatible."
    - "Show output can render normalized joined views while JSON raw mode preserves old source-field contract."
    - "Coverage test accounts for every relationship row and all 34 tables."
  artifacts:
    - path: "openrpg_cli/api.py"
      provides: "Public RulesAPI façade and cached factory"
    - path: "openrpg_cli/__init__.py"
      provides: "Stable public exports"
    - path: "openrpg_cli/cli.py"
      provides: "Compatible show integration with normalized views"
    - path: "tests/test_rules_api.py"
      provides: "Full table/relationship/public compatibility coverage"
  key_links:
    - from: "openrpg_cli/api.py"
      to: "openrpg_cli/character_rules.py"
      via: "character query delegation"
      pattern: "CharacterRules"
    - from: "openrpg_cli/api.py"
      to: "openrpg_cli/combat_rules.py"
      via: "combat query delegation"
      pattern: "CombatRules"
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/api.py"
      via: "normalized show lookup"
      pattern: "get_rules_api"
---

<objective>
Publish one stable normalized rules API, integrate richer CLI inspection, and enforce complete source relationship coverage.

Purpose: Finish Phase 2 as consumable foundation for character/combat engines while retaining Phase 1 behavior.
Output: Public façade/exports, compatible CLI integration, phase-wide coverage tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-normalized-rules-api/02-CONTEXT.md
@.planning/phases/02-normalized-rules-api/02-02-SUMMARY.md
@.planning/phases/02-normalized-rules-api/02-03-SUMMARY.md
@openrpg_cli/__init__.py
@openrpg_cli/cli.py
@tests/test_data.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish stable RulesAPI façade</name>
  <files>openrpg_cli/api.py, openrpg_cli/__init__.py, tests/test_rules_api.py</files>
  <behavior>
    - `get_rules_api()` returns cached read-only façade backed by bundled repository.
    - Façade exposes deterministic list/get methods for class, species, background, feat, spell, creature, and weapon views.
    - Unknown exact identity returns None; ambiguous partial input is never silently chosen.
    - Returned aggregates cannot mutate raw repository rows.
  </behavior>
  <action>Create `RulesAPI` composing character and combat services, with explicit typed public methods and cached `get_rules_api()`. Export stable API/types from package without exporting mutable implementation indexes. Preserve offline-only behavior and stdlib-only addition per D-03. Document exact lookup semantics in docstrings. Avoid generic `dict[str, Any]` in public aggregate returns.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_rules_api.py -x -k public</automated>
  </verify>
  <done>External caller can import one stable API and access all normalized domains through typed immutable returns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate normalized show without breaking CLI</name>
  <files>openrpg_cli/cli.py, tests/test_rules_api.py</files>
  <behavior>
    - Existing categories/list/search/info/audit/roll command contracts remain passing.
    - `show classes|species|backgrounds|feats|creatures|spells|weapons` renders joined child sections.
    - `show --json` retains existing raw source-field payload shape.
    - Normalized rendering has deterministic child order.
  </behavior>
  <action>Enhance non-JSON `show` for supported core categories to use `RulesAPI` aggregate views per D-06 compatibility allowance. Render joined sections using current Rich style. Keep raw JSON path and existing `SRDRepository` list/search/category interfaces unchanged. Add CliRunner regression tests for old commands plus representative Barbarian, Dwarf, Acolyte, Alert, Goblin Warrior, Fireball, and Battleaxe joined output. Do not add new command surface unless needed for compatibility.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_rules_api.py tests/test_data.py tests/test_dice.py -x</automated>
  </verify>
  <done>Joined content visible from show; JSON/raw and all Phase 1 commands remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce phase-wide table and relationship coverage</name>
  <files>tests/test_rules_api.py</files>
  <behavior>
    - Manifest's 34 declared tables equal catalog's 34 classified tables.
    - Every relationship child row appears exactly once in appropriate aggregate.
    - Every foreign key resolves to correct table.
    - Repeated full traversals produce equal ordered tuples.
    - Audit, full pytest, and ruff remain green.
  </behavior>
  <action>Add parameterized coverage matrix across `ClassFeature`, `ClassFeatureItem`, `SpeciesTrait`, `BackgroundBenefit`, `FeatBenefit`, `CreatureTrait`, `CreatureAction`, `CreatureActionAttack`, `WeaponPropertyAssignment`, `SpellCastingOption`, plus `Spell.classes` and subclass/subspecies refs. Assert exact source counts against flattened aggregate memberships, unique child pks, valid parents, deterministic repeated traversal, and explicit catalog classification for remaining reference/internal tables. This implements API-06, D-04, D-05 without hardcoding record counts already owned by manifest.</action>
  <verify>
    <automated>py -3.14 -m pytest -q &amp;&amp; py -3.14 -m openrpg_cli audit &amp;&amp; py -3.14 -m ruff check .</automated>
  </verify>
  <done>All 34 tables accounted for; every relationship row covered; full milestone gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Python caller → RulesAPI | Caller-controlled lookups enter public façade |
| CLI args → normalized lookup/rendering | User input selects bundled records and output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | S | RulesAPI lookup | mitigate | Exact normalized identity semantics; reject ambiguity, no arbitrary partial selection |
| T-02-11 | T | CLI raw/normalized split | mitigate | Keep `--json` on raw repository path; regression-test payload compatibility |
| T-02-12 | D | full traversal | mitigate | Cached bounded indexes and deterministic tuples; no network or unbounded recursion |
| T-02-13 | R | coverage claims | mitigate | Count-based flattened membership tests tied to manifest tables provide reproducible evidence |
</threat_model>

<verification>
`py -3.14 -m openrpg_cli audit`
`py -3.14 -m pytest -q`
`py -3.14 -m ruff check .`
</verification>

<success_criteria>
- One public typed API exposes all Phase 2 aggregates.
- CLI remains compatible and displays joined rules in non-JSON show.
- Every bundled table classified; every relationship row covered exactly once.
- Audit, full tests, ruff pass with no runtime network dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalized-rules-api/02-04-SUMMARY.md`
</output>
