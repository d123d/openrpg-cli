---
phase: 02-normalized-rules-api
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - srd_cli/character_rules.py
  - tests/test_character_rules.py
autonomous: true
requirements:
  - API-02
  - API-03
  - API-05
must_haves:
  truths:
    - "Caller can inspect a class with joined features, level items, proficiencies, starting equipment text, and spellcasting data present in SRD tables."
    - "Caller can inspect species, background, and feat benefits without raw parent keys."
    - "Caller can resolve class spell lists and spell scaling options deterministically."
  artifacts:
    - path: "srd_cli/character_rules.py"
      provides: "Character-facing aggregate views and joins"
    - path: "tests/test_character_rules.py"
      provides: "Class/species/background/feat/spell relationship tests"
  key_links:
    - from: "srd_cli/character_rules.py"
      to: "srd_cli/normalized.py"
      via: "typed loaders and parent indexes"
      pattern: "Normalized"
    - from: "ClassFeatureItem.parent"
      to: "ClassFeature.pk"
      via: "nested class feature items"
      pattern: "feature_items"
    - from: "Spell.classes"
      to: "CharacterClass.pk"
      via: "class spell list"
      pattern: "spells"
---

<objective>
Implement character-facing normalized joins for classes, species, backgrounds, feats, and spells.

Purpose: Expose complete SRD choices and benefits Phase 3 can consume without foreign-key handling.
Output: Immutable aggregate views, query API, exhaustive relationship tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-normalized-rules-api/02-CONTEXT.md
@.planning/phases/02-normalized-rules-api/02-01-SUMMARY.md
@srd_cli/data.py
@srd_cli/data/srd521/CharacterClass.json
@srd_cli/data/srd521/ClassFeature.json
@srd_cli/data/srd521/ClassFeatureItem.json
@srd_cli/data/srd521/SpeciesTrait.json
@srd_cli/data/srd521/BackgroundBenefit.json
@srd_cli/data/srd521/FeatBenefit.json
@srd_cli/data/srd521/Spell.json
@srd_cli/data/srd521/SpellCastingOption.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Join classes, features, level items, and spellcasting</name>
  <files>srd_cli/character_rules.py, tests/test_character_rules.py</files>
  <behavior>
    - Class lookup accepts pk or normalized exact name and returns one immutable aggregate.
    - Aggregate contains ordered direct features; each feature contains ordered level items.
    - Subclasses resolve to parent class without exposing raw subclass_of key as only representation.
    - Class spells resolve from Spell.classes; each spell includes ordered SpellCastingOption rows.
    - Broken feature/item/class/spell parent references raise integrity errors during API construction.
  </behavior>
  <action>Create frozen aggregate dataclasses and `CharacterRules` query service using Plan 01 contracts/index helpers. Join `CharacterClass ← ClassFeature ← ClassFeatureItem`; resolve subclass relationships; derive class spell lists from `Spell.classes`; attach `SpellCastingOption` by parent. Preserve all source-backed proficiency/starting-equipment information from class features/items rather than parsing prose into invented mechanics. Ordering: class/name casefold then pk; features by earliest item level then name/pk; items by level then pk; spells by level/name/pk; options by type/pk. Implement per D-02, D-04 and specific class/spell join requirement.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_rules.py -x -k "class or spell"</automated>
  </verify>
  <done>Every class/subclass resolves features and items; class spell lists/options resolve; no aggregate exposes unresolved relationship as sole value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Join species traits, background benefits, and feat benefits</name>
  <files>srd_cli/character_rules.py, tests/test_character_rules.py</files>
  <behavior>
    - Every species includes traits ordered by source order then pk.
    - Every background includes all benefits ordered by type/name/pk.
    - Every feat includes all benefits ordered by name/pk.
    - Subspecies parent is resolved when present.
    - Orphan benefit/trait rows fail exhaustive integrity validation.
  </behavior>
  <action>Add species, background, and feat aggregate queries per D-02. Join child tables by exact primary/parent keys. Return tuples only. Preserve benefit type, name, description, and source identity; do not interpret benefits into character state (deferred). Add parameterized coverage asserting every child pk appears exactly once under its valid parent and every parent can be queried. Keep no runtime network fallback per project decision.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_rules.py -x</automated>
  </verify>
  <done>All species traits, background benefits, feat benefits resolve exactly once; ordering stable; orphan checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relationship keys → aggregate graph | Source parent/class keys select joined records |
| Query string → entity lookup | Caller-controlled name/pk selects one bundled entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | relationship joins | mitigate | Require every child key resolve exactly once; reject orphans and duplicate assignment |
| T-02-05 | S | entity lookup | mitigate | Exact normalized name/pk matching; ambiguous matches return no entity/error, never arbitrary first hit |
| T-02-06 | D | aggregate construction | mitigate | Index once, tuple outputs, bounded bundled dataset; avoid recursive subclass traversal without cycle check |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_character_rules.py -x`
`py -3.14 -m srd_cli audit`
`py -3.14 -m ruff check srd_cli/character_rules.py tests/test_character_rules.py`
</verification>

<success_criteria>
- Every character-facing relationship table is exhaustively joined.
- Class aggregates contain features/items/spells/options and source proficiency/equipment content.
- Species/background/feat aggregates contain every benefit exactly once.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalized-rules-api/02-02-SUMMARY.md`
</output>
