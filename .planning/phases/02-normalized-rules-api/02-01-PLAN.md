---
phase: 02-normalized-rules-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/models.py
  - openrpg_cli/normalized.py
  - tests/test_normalized_models.py
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "Caller can obtain typed, immutable core SRD entities without changing bundled JSON."
    - "Every one of 34 source tables is explicitly classified as exposed or internal."
    - "Repeated reads return deterministic primary-key ordering."
  artifacts:
    - path: "openrpg_cli/models.py"
      provides: "Frozen dataclass contracts for normalized SRD entities"
    - path: "openrpg_cli/normalized.py"
      provides: "Read-only table catalog, typed conversion, deterministic indexing"
    - path: "tests/test_normalized_models.py"
      provides: "Model immutability, classification, ordering, raw-data preservation tests"
  key_links:
    - from: "openrpg_cli/normalized.py"
      to: "openrpg_cli/data.py"
      via: "SRDRepository.table reads"
      pattern: "SRDRepository"
    - from: "openrpg_cli/normalized.py"
      to: "openrpg_cli/models.py"
      via: "typed row conversion"
      pattern: "from openrpg_cli.models import"
---

<objective>
Create immutable normalized contracts and deterministic read-only foundation above `SRDRepository`.

Purpose: Give later joins stable types, keys, ordering, and complete source-table accountability without touching raw bundle.
Output: Frozen dataclasses, normalized table catalog/index helpers, focused tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-normalized-rules-api/02-CONTEXT.md
@openrpg_cli/data.py
@openrpg_cli/data/srd521/manifest.json

<interfaces>
From openrpg_cli/data.py:
```python
class SRDRepository:
    def table(self, filename: str) -> tuple[dict[str, Any], ...]: ...
    def entries(self, category: str) -> list[dict[str, Any]]: ...
```

New contracts must include identity (`pk`) plus source fields needed by joins for:
`CharacterClass`, `ClassFeature`, `ClassFeatureItem`, `Species`, `SpeciesTrait`,
`Background`, `BackgroundBenefit`, `Feat`, `FeatBenefit`, `Creature`,
`CreatureTrait`, `CreatureAction`, `CreatureActionAttack`, `Weapon`,
`WeaponProperty`, `WeaponPropertyAssignment`, `Spell`, `SpellCastingOption`.
Use tuple-valued collections and frozen dataclasses.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define frozen normalized entity contracts</name>
  <files>openrpg_cli/models.py, tests/test_normalized_models.py</files>
  <behavior>
    - Constructing each core model from representative source fields preserves pk and typed scalar values.
    - List-valued source fields become tuples.
    - Assigning to any model field raises FrozenInstanceError.
    - Nullable source values remain explicitly optional, never replaced by invented gameplay defaults.
  </behavior>
  <action>Create Python 3.11-compatible `@dataclass(frozen=True, slots=True)` contracts for all core and relationship entities listed in interfaces, per D-01 raw bundle preservation and D-03 stdlib preference. Keep source enum-like values as strings unless a closed enum is proven across full table. Include only source-backed fields needed for display, character creation, combat, and joins; retain complete creature/spell gameplay fields using typed mappings/tuples where dozens of sparse named stats would make the contract less stable. Add conversion tests before implementation. Do not modify JSON files or add dependencies.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_normalized_models.py -x</automated>
  </verify>
  <done>All required core entities have importable frozen contracts; representative conversion expectations and mutation rejection pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic table catalog and typed loader</name>
  <files>openrpg_cli/normalized.py, tests/test_normalized_models.py</files>
  <behavior>
    - Catalog keys exactly equal 34 manifest-declared JSON tables.
    - Every catalog entry has explicit exposed/internal disposition and optional model converter.
    - Typed loads sort by stringified primary key and return tuples.
    - Loading normalized entities never mutates repository rows or files.
    - Duplicate primary keys and missing required parent keys fail with table-specific ValueError.
  </behavior>
  <action>Implement normalized API foundation above `SRDRepository`, per D-02. Add explicit 34-table catalog: core/reference tables usable by consumers are exposed; provenance/transform-only tables may be internal but must carry a reason. Read declared filenames from repository manifest only for validation, not dynamic classification. Implement reusable row extraction, deterministic pk ordering, typed loader, immutable pk index, and parent-group index. Validate identity/relationship integrity with clear filename/pk errors. Preserve current `SRDRepository` behavior. Stable ordering required per D-04; all 34 tables classified per D-05.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_normalized_models.py -x</automated>
  </verify>
  <done>Catalog covers exactly 34 tables; typed tuples/indexes deterministic; source rows unchanged; malformed identities rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled JSON → normalized loader | Vendored structured input enters typed API |
| API caller → returned entity | Caller receives data that must remain immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | normalized loader | mitigate | Validate table identity, required keys, duplicate pks, parent key shape before indexing |
| T-02-02 | T | returned models | mitigate | Frozen dataclasses, tuple collections, immutable index views prevent caller mutation |
| T-02-03 | I | table catalog | accept | All content is public SRD; catalog exposes no secrets |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_normalized_models.py -x`
`py -3.14 -m openrpg_cli audit`
`py -3.14 -m ruff check openrpg_cli/models.py openrpg_cli/normalized.py tests/test_normalized_models.py`
</verification>

<success_criteria>
- Frozen typed representations exist for every character/combat core and relationship record.
- Catalog covers every manifest source table with explicit disposition.
- Deterministic loaders return immutable values and leave raw repository rows unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-normalized-rules-api/02-01-SUMMARY.md`
</output>
