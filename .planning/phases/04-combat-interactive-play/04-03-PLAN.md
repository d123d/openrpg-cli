---
phase: 04-combat-interactive-play
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - srd_cli/combat_session.py
  - srd_cli/cli.py
  - tests/test_combat_session.py
  - tests/test_combat_cli.py
autonomous: true
requirements: [COMBAT-05, PLAY-01]
must_haves:
  truths:
    - "Headless combat loads validated character and exact SRD creature, auto-runs to result, and never prompts."
    - "Same character, monster, seed, and format produce byte-identical transcript/JSON."
    - "Interactive combat exposes only currently legal weapon/spell choices and guides turns through result."
    - "Malformed character, unknown monster, unsupported choice, and safety-limit exhaustion fail readably with nonzero status."
  artifacts:
    - path: "srd_cli/combat_session.py"
      provides: "Session orchestration and canonical transcript/JSON serialization"
      exports: ["CombatSession", "render_transcript", "render_combat_json"]
    - path: "srd_cli/cli.py"
      provides: "`srd combat` command"
    - path: "tests/test_combat_cli.py"
      provides: "Subprocess-level headless and interactive combat contract tests"
  key_links:
    - from: "srd_cli/cli.py"
      to: "srd_cli/combat_session.py"
      via: "thin command adapter"
      pattern: "CombatSession"
    - from: "srd_cli/combat_session.py"
      to: "srd_cli/character_store.py"
      via: "validated character load"
      pattern: "CharacterStore"
    - from: "srd_cli/combat_session.py"
      to: "srd_cli/api.py"
      via: "exact SRD creature lookup"
      pattern: "get_creature"
---

<objective>
Expose complete combat through stable headless and guided terminal workflows.

Purpose: Make engine reproducibility observable at process boundary and usable by humans.
Output: Session runner, canonical serializers, and `srd combat`.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-combat-interactive-play/04-CONTEXT.md
@.planning/phases/04-combat-interactive-play/04-02-SUMMARY.md
@srd_cli/character_store.py
@srd_cli/cli.py
@srd_cli/api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate sessions and canonical output</name>
  <files>srd_cli/combat_session.py, tests/test_combat_session.py</files>
  <behavior>
    - Auto policy selects first legal player action in stable type/name/PK order and completes encounter.
    - Canonical JSON uses fixed schema/version, sorted keys, fixed indentation/newline, primitive values, and excludes paths/timestamps/object repr.
    - Text transcript derives solely from canonical events with fixed ordering/format and terminal summary.
    - Same inputs produce exact same bytes; changed seed is represented and can change rolls.
    - Max-round/action guard terminates pathological encounters with explicit error.
  </behavior>
  <action>Per locked stable headless transcript/JSON decision, add CombatSession around engine; orchestration may choose commands but must never mutate state. Auto player policy chooses legal weapon/spell from stable catalog order. Serialize initial identities/stats, seed, ordered events, RNG draw count, final state/outcome under explicit combat transcript schema version. Use json.dumps(sort_keys=True, ensure_ascii=False, indent=2) plus one newline. Text output must not contain memory addresses, locale-dependent formatting, absolute paths, timestamps, or Rich ANSI. Add finite max-round/action guard without altering successful output.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_session.py -q</automated>
  </verify>
  <done>Auto and step sessions share one engine path; JSON/text are canonical, bounded, complete, and byte-reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add combat CLI command</name>
  <files>srd_cli/cli.py, tests/test_combat_cli.py</files>
  <behavior>
    - `srd combat --character FILE --monster NAME --seed N --auto` finishes without stdin and returns 0 on victory or defeat.
    - `--json` emits parseable canonical JSON to stdout and no Rich markup/noise.
    - Without --auto, command displays state and prompts only for current legal player action; enemy turns run automatically.
    - Invalid/tampered character, ambiguous/unknown creature, unsupported action, and session-limit error return exit 2 on stderr.
  </behavior>
  <action>Add top-level `combat` command with required `--character`, `--monster`, `--seed`; flags `--auto`, `--json`; optional bounded `--max-rounds` primarily for tests. Load through CharacterStore validation; resolve exact normalized creature through RulesAPI. In interactive mode render current round/HP/legal action menu and delegate selected command to session. JSON mode must suppress progress/table output. Follow existing thin CLI and domain-error exit-2 pattern. Keep runtime offline stdlib+Typer+Rich per locked runtime decision; add no dependency.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_cli.py tests/test_character_cli.py -q</automated>
  </verify>
  <done>Headless command meets required invocation and byte identity; interactive combat completes using same session/engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem character → session | User-controlled JSON enters combat. |
| CLI args/stdin → session | Paths, monster selectors, seeds, and action selections are untrusted. |
| Engine events → stdout | Stable machine output must not leak local/environment data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | CombatSession load | mitigate | Load only through CharacterStore integrity validation and exact RulesAPI creature lookup. |
| T-04-08 | Information Disclosure | Serializers | mitigate | Emit allowlisted primitive schema; exclude paths, timestamps, repr, and environment state. |
| T-04-09 | Denial of Service | Auto/session loop | mitigate | Enforce positive bounded max rounds/actions and explicit failure. |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_combat_session.py tests/test_combat_cli.py tests/test_character_cli.py -q`
</verification>

<success_criteria>
Required headless invocation runs to completion without input and both output formats are byte-identical for identical inputs; humans can choose legal actions interactively.
</success_criteria>

<output>
After completion, create `.planning/phases/04-combat-interactive-play/04-03-SUMMARY.md`
</output>

