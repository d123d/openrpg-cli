---
phase: 04-combat-interactive-play
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - openrpg_cli/combat_actions.py
  - openrpg_cli/combat.py
  - tests/test_combat_actions.py
autonomous: true
requirements: [COMBAT-03, COMBAT-04]
must_haves:
  truths:
    - "Player can choose every supported offensive prepared spell; unsupported spells fail with a readable reason and no state/RNG change."
    - "Attack-roll and save-based direct damage spells resolve from structured SRD fields."
    - "Enemy chooses a legal damaging joined SRD action deterministically and resolves its structured attack."
    - "Creature with no resolvable damaging action alone receives rules-neutral unarmed fallback."
  artifacts:
    - path: "openrpg_cli/combat_actions.py"
      provides: "Bounded spell adapters and deterministic creature action resolver"
      exports: ["CombatActionCatalog", "UnsupportedCombatAction"]
    - path: "tests/test_combat_actions.py"
      provides: "Spell/action legality, CR-band, fallback, and deterministic resolution tests"
  key_links:
    - from: "openrpg_cli/combat_actions.py"
      to: "openrpg_cli/combat_rules.py"
      via: "CreatureView.actions and CreatureActionAttack structured joins"
      pattern: "CreatureView|CreatureActionView|CreatureActionAttack"
    - from: "openrpg_cli/combat_actions.py"
      to: "openrpg_cli/character_rules.py"
      via: "SpellView structured attack_roll, saving_throw_ability, and damage_roll"
      pattern: "SpellView"
    - from: "openrpg_cli/combat.py"
      to: "openrpg_cli/combat_actions.py"
      via: "validated player spell and enemy action commands"
      pattern: "CombatActionCatalog"
---

<objective>
Add bounded structured spell combat and deterministic SRD creature turns.

Purpose: Complete required player/enemy action surface without parsing prose or inventing mechanics.
Output: Tested action catalog/resolvers wired into combat state machine.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-combat-interactive-play/04-CONTEXT.md
@.planning/phases/04-combat-interactive-play/04-01-SUMMARY.md
@openrpg_cli/combat_rules.py
@openrpg_cli/character_rules.py
@openrpg_cli/models.py

<interfaces>
From openrpg_cli/combat_rules.py:
```python
@dataclass(frozen=True, slots=True)
class CreatureActionView:
    action: CreatureAction
    attacks: tuple[CreatureActionAttack, ...]

@dataclass(frozen=True, slots=True)
class CreatureView:
    entity: Creature
    traits: tuple[CreatureTrait, ...]
    actions: tuple[CreatureActionView, ...]
```

Structured spell fields: `attack_roll`, `saving_throw_ability`, `damage_roll`, `damage_types`, `target_count`, `casting_time`.
Structured creature attack fields: `to_hit_mod`, `damage_die_count`, `damage_die_type`, `damage_bonus`, optional extra-damage fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build explicit offensive spell resolver catalog</name>
  <files>openrpg_cli/combat_actions.py, openrpg_cli/combat.py, tests/test_combat_actions.py</files>
  <behavior>
    - Prepared single-target action spell with structured damage plus attack_roll resolves against AC using character spell attack bonus.
    - Prepared single-target action spell with structured damage plus saving_throw_ability rolls target save against character spell save DC and applies full or half damage only when policy explicitly supports it.
    - Missing/ambiguous damage, ongoing damage, multi-target, area, condition-only, concentration-only, healing, or prose-dependent mechanics return stable unsupported reason.
    - Unsupported/unprepared spell consumes no RNG and changes no state.
  </behavior>
  <action>Per locked bounded structured-spell decision, implement explicit eligibility classification from Spell/SpellCastingOption fields. Support direct instantaneous single-target `casting_time=action` damage spells where attack-roll or save semantics and damage expression are structured; define exact half-on-save support using a named allowlist derived from SRD spell identity where structured fields lack success semantics. Reject delayed/ongoing, multi-target/area, rider/condition, concentration, healing, non-damage, or missing-mechanics spells with stable reason. Never regex prose into mechanics. Wire legal prepared Character.spells to CombatEngine spell commands and canonical events.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_actions.py -q</automated>
  </verify>
  <done>Supported spell set is mechanically derived/allowlisted, queryable for UI, deterministic, and all unsupported prepared spells explain rejection atomically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select and resolve deterministic enemy actions</name>
  <files>openrpg_cli/combat_actions.py, openrpg_cli/combat.py, tests/test_combat_actions.py</files>
  <behavior>
    - Joined damaging ACTION attacks become legal candidates; legendary/reaction/non-damaging/prose-only entries do not.
    - Same state/seed chooses same candidate and yields same rolls/damage.
    - Candidate order is stable by stat-block order/name/PK; RNG selection uses only explicit engine RNG.
    - Unarmed fallback occurs only when candidate list is empty.
    - Representative low, medium, and high CR creatures either resolve a structured action or receive documented fallback.
  </behavior>
  <action>Per locked deterministic enemy-action decision, convert CreatureActionAttack rows into attack bonus/damage expressions, including structured extra damage when present. Limit candidates to ordinary ACTION attacks usable in one movement-neutral turn; reject legendary/reaction, recharge/limited-use, multiattack prose, saves/conditions lacking structured resolution. Use stable candidate ordering then engine RNG selection. Add rules-neutral unarmed strike (ability-derived attack, 1 + STR modifier minimum 1 damage) only for zero candidates; emit fallback reason. Test multiple CR bands and exact candidate stability.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_actions.py tests/test_combat_engine.py -q</automated>
  </verify>
  <done>Enemy turns always choose legal resolvable action deterministically; fallback is narrow, visible, and never masks usable joined attacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SRD structured records → executable action | Data fields become dice expressions, bonuses, saves, and damage. |
| User spell selector → engine | User may request unprepared or unsupported spell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Spell/action catalog | mitigate | Require exact character/SRD identity and eligibility classification before RNG use. |
| T-04-05 | Elevation of Privilege | Enemy action selection | mitigate | Exclude legendary/reaction/limited actions and prose-only mechanics from ordinary turn candidates. |
| T-04-06 | Denial of Service | Source dice expressions | mitigate | Feed expressions through bounded dice parser; reject malformed source row readably. |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_combat_actions.py tests/test_combat_engine.py -q`
</verification>

<success_criteria>
Player structured offensive spells and enemy structured SRD actions resolve deterministically; unsupported mechanics never get invented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-combat-interactive-play/04-02-SUMMARY.md`
</output>

