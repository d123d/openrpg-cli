---
phase: 04-combat-interactive-play
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/combat.py
  - tests/test_combat_engine.py
autonomous: true
requirements: [COMBAT-01, COMBAT-02]
must_haves:
  truths:
    - "Seeded initiative creates one stable movement-neutral turn order."
    - "Player weapon attacks resolve hit, miss, natural 1, critical damage, HP loss, defeat, and victory without CLI state mutation."
    - "Combat state is immutable outside engine transitions and contains enough data to replay every transition."
  artifacts:
    - path: "openrpg_cli/combat.py"
      provides: "Combatant, encounter state, command/event contracts, initiative, and weapon attack transitions"
      exports: ["CombatEngine", "CombatState", "CombatEvent", "CombatError"]
    - path: "tests/test_combat_engine.py"
      provides: "Deterministic initiative and weapon-resolution coverage"
  key_links:
    - from: "openrpg_cli/combat.py"
      to: "openrpg_cli/dice.py"
      via: "single explicit GameRNG owned by CombatEngine"
      pattern: "GameRNG|roll_d20|roll_damage_with_crit"
    - from: "openrpg_cli/combat.py"
      to: "openrpg_cli/character.py"
      via: "Character-derived HP, AC, initiative modifier, and AttackSummary"
      pattern: "Character|AttackSummary"
---

<objective>
Create deterministic combat state machine and player weapon resolution.

Purpose: Establish sole authority for combat state/RNG before spell, enemy, or CLI adapters.
Output: Tested immutable contracts plus initiative and weapon-attack transitions.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-combat-interactive-play/04-CONTEXT.md
@openrpg_cli/character.py
@openrpg_cli/dice.py
@openrpg_cli/combat_rules.py

<interfaces>
From openrpg_cli/character.py:
```python
@dataclass(frozen=True, slots=True)
class AttackSummary:
    weapon: EntityRef
    ability: str
    attack_bonus: int
    damage: str

@dataclass(frozen=True, slots=True)
class DerivedStats:
    modifiers: Mapping[str, int]
    proficiency_bonus: int
    max_hp: int
    current_hp: int
    armor_class: int
    attacks: tuple[AttackSummary, ...] = ()
```

From openrpg_cli/dice.py:
```python
class GameRNG:
    seed: int
    def snapshot(self) -> dict: ...
    def to_dict(self) -> dict: ...

def roll_d20(rng: GameRNG, modifier: int = 0, advantage: Advantage = "normal") -> D20Roll: ...
def roll_damage_with_crit(rng: GameRNG, expression: str, critical: bool) -> RollResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define combat contracts and deterministic initiative</name>
  <files>openrpg_cli/combat.py, tests/test_combat_engine.py</files>
  <behavior>
    - Same seed and combatants produce identical initiative rolls, tie breaks, active actor, round, and RNG state.
    - Natural initiative ties resolve by modifier then stable combatant ID, never collection insertion accident.
    - Only active, non-defeated actor may submit a command; invalid actor/action raises CombatError without consuming RNG or changing state.
    - Advancing after each legal action skips defeated actors, increments round at wrap, and stops after victory/defeat.
  </behavior>
  <action>Per locked decision that engine owns all state and RNG, create frozen Combatant/CombatState/CombatEvent and explicit command/result contracts. Build player from validated Character and enemy from CreatureView using current HP/AC/initiative fields. CombatEngine must accept explicit seed/GameRNG, retain no module-global randomness, return new state plus ordered events for every transition, and implement movement-neutral one-action turns. Define stable IDs (`player`, creature PK), terminal outcome enum, condition tuple, round/turn index, and RNG snapshot. Do not add maps, movement, parties, adventure state, or CLI rendering.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_engine.py -q</automated>
  </verify>
  <done>Initiative/turn lifecycle is deterministic, invalid commands are atomic, terminal encounters cannot advance, contracts are immutable and replay-capable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve player weapon attacks and terminal outcomes</name>
  <files>openrpg_cli/combat.py, tests/test_combat_engine.py</files>
  <behavior>
    - Natural 1 always misses; natural 20 always hits and doubles damage dice once while applying flat modifier once.
    - Normal attack hits when total meets AC, applies bounded damage, never lowers HP below zero, and emits roll/damage events.
    - Zero HP marks combatant defeated and yields player victory or defeat immediately.
    - Unknown/non-equipped weapon is rejected before RNG draw.
  </behavior>
  <action>Implement player weapon command from Character.derived.attacks per locked combat-scope decision. Reuse roll_d20 and roll_damage_with_crit; never duplicate dice parsing or critical logic. Validate selected AttackSummary belongs to player, emit canonical primitive event payloads containing natural/total/AC/damage/HP, and apply state only after full validation. Support only conditions needed by later structured actions; define condition storage now without inventing condition mechanics.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_combat_engine.py -q &amp;&amp; py -3.14 -m pytest tests/test_dice.py tests/test_character_rules.py -q</automated>
  </verify>
  <done>Equipped weapons fully resolve hit/miss/crit/damage/defeat/victory with exact seeded events and no hidden RNG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Character/RulesAPI → engine | Persisted character and SRD-derived combat values enter state machine. |
| Command → transition | Untrusted actor/action selectors may attempt illegal or out-of-turn state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CombatEngine command validation | mitigate | Validate actor, turn, ownership, legality, and terminal state before any RNG draw/state transition. |
| T-04-02 | Repudiation | Combat events | mitigate | Emit ordered canonical events with round, actor, rolls, deltas, and outcome. |
| T-04-03 | Denial of Service | Damage expressions | mitigate | Delegate to bounded dice parser and reject illegal weapon references before rolling. |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_combat_engine.py tests/test_dice.py tests/test_character_rules.py -q`
</verification>

<success_criteria>
One-character combat has reproducible initiative, legal turn enforcement, complete weapon resolution, and terminal outcomes entirely inside engine.
</success_criteria>

<output>
After completion, create `.planning/phases/04-combat-interactive-play/04-01-SUMMARY.md`
</output>

