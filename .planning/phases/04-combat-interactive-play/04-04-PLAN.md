---
phase: 04-combat-interactive-play
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - openrpg_cli/cli.py
  - tests/test_play_cli.py
  - tests/test_release_smoke.py
  - README.md
autonomous: true
requirements: [PLAY-01, PLAY-02, COMBAT-01, COMBAT-02, COMBAT-03, COMBAT-04, COMBAT-05]
must_haves:
  truths:
    - "User can run `srd play`, create or load a valid character, select an SRD creature, finish encounter, see result, and optionally save character."
    - "Installed wheel contains audited SRD 5.2.1 data and all character/combat commands work outside source tree."
    - "Audit, full pytest, Ruff, build/install, command smoke, and reproducibility gates all pass for v1.0."
  artifacts:
    - path: "openrpg_cli/cli.py"
      provides: "`srd play` end-to-end interactive workflow"
    - path: "tests/test_play_cli.py"
      provides: "Create/load → encounter → result → optional save workflow tests"
    - path: "tests/test_release_smoke.py"
      provides: "Installed-wheel audit and CLI smoke harness"
    - path: "README.md"
      provides: "Combat/play usage, supported spell boundary, deterministic examples, release verification"
  key_links:
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/character_builder.py"
      via: "play create branch reuses character services"
      pattern: "CharacterBuilder"
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/combat_session.py"
      via: "play encounter branch reuses CombatSession"
      pattern: "CombatSession"
    - from: "pyproject.toml"
      to: "openrpg_cli/data/srd521"
      via: "wheel force-include verified after install"
      pattern: "force-include"
---

<objective>
Finish interactive product loop and prove v1.0 from installed artifact.

Purpose: Close user workflow, packaging, content-purity, determinism, and quality hard gates.
Output: `srd play`, release smoke tests, operator docs, passing complete verification.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-combat-interactive-play/04-CONTEXT.md
@.planning/phases/04-combat-interactive-play/04-03-SUMMARY.md
@README.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build complete interactive play loop</name>
  <files>openrpg_cli/cli.py, tests/test_play_cli.py</files>
  <behavior>
    - Play offers create or load; create delegates same legal guided character workflow, load validates persisted character.
    - Monster selection uses stable SRD creature list with readable retry/suggestions.
    - Encounter delegates same interactive CombatSession path and prints terminal result.
    - Optional save uses CharacterStore overwrite protections and never writes unless user confirms.
    - EOF/cancel and bad choice exit readably without traceback.
  </behavior>
  <action>Per locked interactive play workflow decision, add top-level `play` command as orchestration only. Reuse CharacterBuilder/CharacterStore/RulesAPI/CombatSession rather than duplicating validation or mechanics. Prompt in deterministic menu order, expose seed input/default explicitly, and preserve source character unless user requests save. No adventure, setting, NPC, quest, lore, progression, party, or map state per locked scope boundary and deferred list. Tests use Typer CliRunner scripted stdin for both create and load paths through terminal result.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_play_cli.py tests/test_combat_cli.py tests/test_character_cli.py -q</automated>
  </verify>
  <done>`srd play` completes both create and load flows end-to-end while all mechanics remain delegated to tested services.</done>
</task>

<task type="auto">
  <name>Task 2: Verify installed artifact and document supported product</name>
  <files>tests/test_release_smoke.py, README.md</files>
  <action>Add release smoke test/helper that builds wheel into pytest tmp path, installs it with `py -3.14 -m pip install --no-deps --target` into isolated target, then launches subprocess from a temp working directory with PYTHONPATH set only to installed target. Verify `python -m openrpg_cli audit`, info, deterministic roll, character command help, combat help, and play help; verify bundled manifest/data/license availability. Add README quickstart for character→combat headless command, play flow, JSON reproducibility, supported structured offensive-spell boundary/readable unsupported behavior, offline guarantee, SRD-only scope, and exact release gate commands. Do not loosen manifest/audit or add network/download steps.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_release_smoke.py -q</automated>
  </verify>
  <done>Built wheel works outside checkout, audits bundled content, exposes full CLI, and README states exact supported mechanics/boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Run v1.0 full verification matrix</name>
  <files>tests/test_release_smoke.py</files>
  <action>Run all milestone hard gates. Ensure tests include subprocess byte comparison for two identical seeded auto combats plus representative low/medium/high CR encounters. Fix only Phase 4 defects or direct regressions found; never alter SRD data/manifest to bypass audit and never weaken assertions/gates. Capture exact passing counts/commands in summary.</action>
  <verify>
    <automated>py -3.14 -m openrpg_cli audit &amp;&amp; py -3.14 -m pytest -q &amp;&amp; py -3.14 -m ruff check . &amp;&amp; py -3.14 -m build &amp;&amp; py -3.14 -m pytest tests/test_release_smoke.py -q</automated>
  </verify>
  <done>Audit, complete tests, Ruff, wheel build/install smoke, offline command surface, CR-band combat, and seeded byte identity all pass together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interactive input → services | Menu choices, paths, names, and save decisions are untrusted. |
| Built wheel → runtime | Packaging may omit audited data/license or accidentally depend on checkout/network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | Play save flow | mitigate | Reuse CharacterStore validation and overwrite confirmation; never direct-write JSON. |
| T-04-11 | Information Disclosure | Play errors/output | mitigate | Convert expected domain/path errors to readable messages without traceback or local internals. |
| T-04-12 | Tampering | Installed content bundle | mitigate | Run manifest audit from isolated installed target and verify attribution/data presence. |
| T-04-13 | Denial of Service | Interactive loop | mitigate | Reuse bounded combat session and handle EOF/cancel as controlled exit. |
</threat_model>

<verification>
`py -3.14 -m openrpg_cli audit`
`py -3.14 -m pytest -q`
`py -3.14 -m ruff check .`
`py -3.14 -m build`
`py -3.14 -m pytest tests/test_release_smoke.py -q`
</verification>

<success_criteria>
Complete create/load→combat→result workflow works; isolated installed wheel remains audited/offline; every v1.0 hard gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-combat-interactive-play/04-04-SUMMARY.md`
</output>

