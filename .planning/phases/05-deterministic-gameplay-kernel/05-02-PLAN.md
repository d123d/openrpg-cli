---
phase: 05-deterministic-gameplay-kernel
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - openrpg_cli/domain/codecs.py
  - openrpg_cli/engine/__init__.py
  - openrpg_cli/engine/rng.py
  - openrpg_cli/engine/reducer.py
  - tests/test_kernel_reducer.py
autonomous: true
requirements: [KERN-02, KERN-03]
must_haves:
  truths:
    - "Equivalent state, commands, and events always encode to byte-identical canonical JSON."
    - "Reducer output depends only on prior state, command, and explicit RNG state."
    - "Rejected commands leave state and RNG unchanged and return typed validation events."
  artifacts:
    - path: "openrpg_cli/domain/codecs.py"
      provides: "Canonical state/command/event JSON codecs"
    - path: "openrpg_cli/engine/rng.py"
      provides: "Explicit serializable deterministic RNG"
    - path: "openrpg_cli/engine/reducer.py"
      provides: "Pure command reducer and reducer registry"
  key_links:
    - from: "openrpg_cli/engine/reducer.py"
      to: "openrpg_cli/domain/messages.py"
      via: "typed command dispatch and event emission"
      pattern: "Command"
    - from: "openrpg_cli/engine/reducer.py"
      to: "openrpg_cli/engine/rng.py"
      via: "RNG passed and returned with reduction result"
      pattern: "GameRNG"
---

<objective>
Build canonical codecs and pure deterministic reduction core.

Purpose: Make authoritative mutation replayable and prevent interfaces from owning randomness or state transitions.
Output: Canonical JSON, explicit RNG, pure reducer, deterministic differential tests.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deterministic-gameplay-kernel/05-CONTEXT.md
@.planning/phases/05-deterministic-gameplay-kernel/05-01-SUMMARY.md
@openrpg_cli/dice.py
@openrpg_cli/combat.py

<interfaces>
Use contracts created by Plan 01 directly:
```python
GameState
Command
Event
RuntimeContentRef
```
Existing combat owns `random.Random` internally; new kernel must not copy that pattern. GameRNG is explicit input/output per D-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict canonical JSON codecs</name>
  <files>tests/test_kernel_reducer.py, openrpg_cli/domain/codecs.py</files>
  <behavior>
    - Dict insertion order cannot affect encoded bytes.
    - Decode rejects duplicate keys, unknown fields/tags/schema versions, non-finite numbers, oversized payloads, and excessive nesting.
    - encode(decode(encode(value))) is byte-identical for state, every command, and every event variant.
  </behavior>
  <action>Implement explicit codecs per KERN-02 and D-03: UTF-8, sorted keys, compact separators, stable enum/tuple representation, no NaN/Infinity, and explicit schema/type discriminators. Decode through allowlisted constructors only—never pickle, eval, dynamic imports, or arbitrary class names. Enforce D-07 byte/depth caps before/while decoding. Test all registered tags so adding a variant without codec coverage fails.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_reducer.py -q -k codec</automated>
  </verify>
  <done>All kernel values have strict round-trip codecs and canonical bytes independent of Python mapping order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit RNG and pure reducer</name>
  <files>tests/test_kernel_reducer.py, openrpg_cli/engine/__init__.py, openrpg_cli/engine/rng.py, openrpg_cli/engine/reducer.py</files>
  <behavior>
    - Same state + command + RNG produces equal next state, events, and RNG.
    - Input state and RNG remain unchanged.
    - RNG consumption is visible only in returned RNG state and emitted roll/result event data.
    - Unknown/invalid/out-of-order commands return typed rejection without state mutation.
  </behavior>
  <action>Create serializable GameRNG with deterministic integer/die primitives; avoid global random and interface-generated rolls. Create ReductionResult(state, events, rng) and pure reduce_command function per KERN-03, D-02. Dispatch through explicit allowlisted handler registry. Implement only kernel lifecycle commands needed to prove state entity creation/update and runtime scene attachment; no checks, attacks, effects resolution, action economy, or other deferred Phase 6–7 semantics. Reducer returns replacements, never mutates inputs. Validation failures become stable typed rejection events.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_reducer.py -q</automated>
  </verify>
  <done>Reducer is referentially deterministic, consumes explicit RNG, emits typed events, and no interface-side mutation/randomness is required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON → typed kernel values | Untrusted serialized commands/state enter constructors |
| caller → reducer registry | Caller attempts illegal tag, transition, or RNG manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-05 | T/E | codecs.py | mitigate | Strict fields, tags, versions, finite numbers, allowlisted constructors |
| T-05-06 | D | codecs.py | mitigate | Payload byte/depth caps and duplicate-key rejection |
| T-05-07 | R | reducer.py | mitigate | Every accepted/rejected command emits stable typed evidence |
| T-05-08 | T | rng.py | mitigate | Explicit immutable RNG state; no module-global entropy |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_kernel_reducer.py -q` and `py -3.14 -m ruff check openrpg_cli/domain openrpg_cli/engine tests/test_kernel_reducer.py` pass.
</verification>

<success_criteria>
Canonical bytes are stable; duplicate reductions match; input values remain unchanged; malformed data fails closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-deterministic-gameplay-kernel/05-02-SUMMARY.md`
</output>
