---
phase: 05-deterministic-gameplay-kernel
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - srd_cli/interfaces/__init__.py
  - srd_cli/interfaces/v1_compat.py
  - srd_cli/combat_session.py
  - tests/test_kernel_compat.py
  - tests/test_release_smoke.py
autonomous: true
requirements: [KERN-03, KERN-04, KERN-05]
must_haves:
  truths:
    - "Existing v1 combat, play, character, browse, audit, and JSON/text outputs remain compatible."
    - "Compatibility adapter records existing combat sessions through kernel log without interface mutation of authoritative kernel state."
    - "Audit, full tests, Ruff, deterministic replay, build, and isolated wheel gates pass."
  artifacts:
    - path: "srd_cli/interfaces/v1_compat.py"
      provides: "One-way legacy-to-kernel compatibility adapters"
    - path: "tests/test_kernel_compat.py"
      provides: "v1 output parity and mutation-boundary regression tests"
    - path: "tests/test_release_smoke.py"
      provides: "Packaged kernel import and isolated compatibility gate"
  key_links:
    - from: "srd_cli/combat_session.py"
      to: "srd_cli/interfaces/v1_compat.py"
      via: "legacy session events translated into kernel log records"
      pattern: "v1_compat"
    - from: "srd_cli/interfaces/v1_compat.py"
      to: "srd_cli/application/log.py"
      via: "typed adapter commands/events appended to SessionLog"
      pattern: "SessionLog"
---

<objective>
Bridge v1 gameplay into kernel audit trail and run full compatibility/release gates.

Purpose: Land foundation without breaking shipped CLI/API while making migration path explicit.
Output: Compatibility adapter, v1 parity tests, deterministic replay gate, isolated wheel proof.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deterministic-gameplay-kernel/05-CONTEXT.md
@.planning/phases/04-combat-interactive-play/04-03-SUMMARY.md
@.planning/phases/04-combat-interactive-play/04-04-SUMMARY.md
@.planning/phases/05-deterministic-gameplay-kernel/05-03-SUMMARY.md
@srd_cli/combat.py
@srd_cli/combat_session.py
@srd_cli/cli.py
@tests/test_combat_cli.py
@tests/test_play_cli.py
@tests/test_release_smoke.py

<interfaces>
```python
@dataclass(frozen=True, slots=True)
class CombatResult:
    state: CombatState
    events: tuple[CombatEvent, ...]

class CombatSession:
    def run_auto(self) -> CombatResult: ...

def render_combat_json(result: CombatResult) -> str: ...
def render_transcript(result: CombatResult) -> str: ...
```
Preserve these signatures and rendered payloads. Adapter dependency direction is interfaces → application → engine → domain; lower layers never import legacy CLI/interface modules.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-way v1 compatibility adapter</name>
  <files>tests/test_kernel_compat.py, srd_cli/interfaces/__init__.py, srd_cli/interfaces/v1_compat.py, srd_cli/combat_session.py</files>
  <behavior>
    - Fixed-seed v1 sessions retain identical CombatResult, transcript, and JSON output.
    - Adapter converts legacy session lifecycle/events to typed kernel records with stable provenance and hashes.
    - Replaying adapter-produced log yields identical kernel snapshot hash.
    - Mutating returned/interface data cannot mutate authoritative kernel state or prior log records.
  </behavior>
  <action>Implement D-01 layer direction and D-06 compatibility as a one-way adapter at interfaces layer. Translate existing CombatState/CombatEvent/Character identifiers into kernel lifecycle commands/events and append them through application API; do not make domain/engine/application import combat_session or CLI. Wire CombatSession internally while preserving constructor, run_auto return type, renderers, CLI arguments, exit codes, JSON field names/order, transcript text, and seeded v1 outcomes. Treat legacy mechanics as recorded adapter results only; do not reimplement Phase 6–7 rules in kernel or claim full universal resolution.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_compat.py tests/test_combat_engine.py tests/test_combat_session.py tests/test_combat_cli.py tests/test_play_cli.py -q</automated>
  </verify>
  <done>Legacy sessions produce same public results while also yielding valid replayable kernel history through dependency-correct adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Gate deterministic kernel and packaged v1 compatibility</name>
  <files>tests/test_kernel_compat.py, tests/test_release_smoke.py</files>
  <action>Add subprocess differential test running same fixture twice in fresh interpreters and comparing canonical final state/event/log hashes byte-for-byte. Extend release smoke to import domain/engine/application/interface packages from isolated installed wheel and run existing audit plus representative v1 commands. Run full hard gates: audit, pytest, Ruff, build, install wheel into temporary target outside checkout, then run isolated audit/kernel replay/v1 smoke with cwd outside repo. Verify wheel contains no runtime user payload, path, playlog, snapshot, or new non-SRD content. Do not weaken existing tests or normalize away output differences.</action>
  <verify>
    <automated>py -3.14 -m srd_cli audit; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest -q; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m ruff check .; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m build</automated>
  </verify>
  <done>All source gates pass; fresh-process hashes match; isolated wheel imports kernel, audits SRD data, replays fixture, and preserves representative v1 CLI behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy mutable interface values → kernel adapter | Existing API data enters authoritative audit trail |
| source checkout → release wheel | Local runtime/user files could leak into distribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-13 | T | v1_compat.py | mitigate | Copy/validate into immutable typed contracts; no shared mutable references |
| T-05-14 | R | v1_compat.py | mitigate | Stable adapter provenance and hash-chained record for every translated action |
| T-05-15 | I | wheel contents | mitigate | Inspect isolated artifact; reject runtime payload/log/snapshot inclusion |
| T-05-16 | E | dependency graph | mitigate | Enforce one-way interfaces→application→engine→domain imports in tests |
</threat_model>

<verification>
Full hard-gate command passes. Isolated wheel smoke runs outside checkout. Existing v1 CLI/API regression tests remain unchanged and green.
</verification>

<success_criteria>
Kernel foundation is replayable and packaged; compatibility adapter preserves shipped behavior; every roadmap hard gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-deterministic-gameplay-kernel/05-04-SUMMARY.md`
</output>
