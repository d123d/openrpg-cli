---
phase: 05-deterministic-gameplay-kernel
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - srd_cli/application/__init__.py
  - srd_cli/application/log.py
  - srd_cli/application/replay.py
  - srd_cli/application/migrations.py
  - tests/test_kernel_replay.py
autonomous: true
requirements: [KERN-04]
must_haves:
  truths:
    - "Session history is append-only and each record authenticates predecessor plus canonical command/events."
    - "Replay from genesis or a verified snapshot produces byte-identical final state and event hashes."
    - "Only allowlisted, stepwise schema migrations can load older saves."
  artifacts:
    - path: "srd_cli/application/log.py"
      provides: "Bounded append-only hash-chained session log"
    - path: "srd_cli/application/replay.py"
      provides: "Genesis/snapshot replay and hash verification"
    - path: "srd_cli/application/migrations.py"
      provides: "Explicit save migration registry"
  key_links:
    - from: "srd_cli/application/replay.py"
      to: "srd_cli/engine/reducer.py"
      via: "reapply canonical commands with recorded RNG lineage"
      pattern: "reduce_command"
    - from: "srd_cli/application/log.py"
      to: "srd_cli/domain/codecs.py"
      via: "hash canonical bytes"
      pattern: "sha256"
---

<objective>
Create tamper-evident logs, deterministic replay, snapshots, and safe migrations.

Purpose: Make sessions durable, auditable, recoverable, and forward-compatible.
Output: Hash-chained log, replay/snapshot service, migration allowlist, corruption/limit tests.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-deterministic-gameplay-kernel/05-CONTEXT.md
@.planning/phases/05-deterministic-gameplay-kernel/05-02-SUMMARY.md
@srd_cli/character_store.py

<interfaces>
```python
# Existing safe persistence precedent
MAX_CHARACTER_BYTES = 1_000_000
class CharacterCodec:
    def encode(self, character: Character) -> bytes: ...
    def decode(self, payload: bytes) -> Character: ...
```
Use Plan 02 canonical codecs, GameRNG, ReductionResult, and reduce_command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build append-only hash-chained log</name>
  <files>tests/test_kernel_replay.py, srd_cli/application/__init__.py, srd_cli/application/log.py</files>
  <behavior>
    - Append calculates record hash from prior hash plus canonical command/events/RNG metadata.
    - Editing, deleting, reordering, or inserting a record makes validation fail at exact index.
    - Event-count, record-count, and total-byte caps reject oversized sessions.
    - Existing records expose immutable tuple views and cannot be replaced through public API.
  </behavior>
  <action>Implement SessionLog and LogRecord per KERN-04/D-04 using canonical codec bytes and SHA-256 hash chaining. Include log schema version, monotonic sequence, command, resulting events, RNG before/after, state hash, previous hash, record hash. Keep append-only public surface; validation recomputes complete chain. Apply named D-07 bounds for events per command, total records, and encoded bytes. File persistence, if included, must use validated safe path plus atomic replace for whole-file save without offering record mutation.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_replay.py -q -k "log or chain or bound"</automated>
  </verify>
  <done>Session log is append-only, bounded, canonical, and detects every tested tamper class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replay, snapshot hashes, and migration allowlist</name>
  <files>tests/test_kernel_replay.py, srd_cli/application/replay.py, srd_cli/application/migrations.py</files>
  <behavior>
    - Genesis replay matches recorded final canonical state bytes, state hash, and event hash.
    - Verified snapshot replay matches genesis replay; altered/incompatible snapshot fails before reduction.
    - Migration applies every adjacent version exactly once and rejects gaps, downgrades, cycles, unknown versions, and post-migration invalid state.
  </behavior>
  <action>Replay by validating chain then invoking reducer for each recorded command with recorded RNG lineage; compare generated events/RNG/state hash at every record and fail on first differential. Define Snapshot with schema version, log position/hash, state/RNG canonical bytes and SHA-256 hashes. Resume only after snapshot and prefix-chain verification. Create explicit adjacent-version migration registry per D-07; migrations are pure functions selected from allowlist, then strict current codec validation runs. Include one synthetic legacy fixture in tests to prove pipeline without inventing unsupported production history.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_replay.py -q</automated>
  </verify>
  <done>Genesis and snapshot replay are byte-identical; corruption pinpoints record; migration accepts only validated allowlisted paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted log/snapshot → replay | Attacker-controlled history could forge state |
| old save → migration | Legacy payload could select unsafe code or bypass current validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-09 | T/R | log.py | mitigate | SHA-256 predecessor chain plus canonical command/event/state hashes |
| T-05-10 | D | log.py | mitigate | Record, event, byte caps validated before append/load |
| T-05-11 | T/E | migrations.py | mitigate | Static adjacent-version function allowlist; reject arbitrary names/imports |
| T-05-12 | T | replay.py | mitigate | Verify snapshot anchor and differential-check every replay result |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_kernel_replay.py -q` and `py -3.14 -m ruff check srd_cli/application tests/test_kernel_replay.py` pass.
</verification>

<success_criteria>
Replay produces byte-identical hashes from genesis and snapshot; tampering, resource exhaustion, and unknown migrations fail closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-deterministic-gameplay-kernel/05-03-SUMMARY.md`
</output>
