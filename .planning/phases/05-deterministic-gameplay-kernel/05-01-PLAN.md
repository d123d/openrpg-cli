---
phase: 05-deterministic-gameplay-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/domain/__init__.py
  - openrpg_cli/domain/state.py
  - openrpg_cli/domain/messages.py
  - openrpg_cli/domain/provenance.py
  - tests/test_kernel_contracts.py
autonomous: true
requirements: [KERN-01, KERN-02, KERN-05]
must_haves:
  truths:
    - "Kernel state is immutable, schema-versioned, and models games, actors, teams, scenes, encounters, clocks, resources, and effects."
    - "Commands and events are closed tagged unions with explicit schema versions."
    - "Runtime scene payloads are labeled user-authored and cannot resolve inside bundled SRD data root."
  artifacts:
    - path: "openrpg_cli/domain/state.py"
      provides: "Immutable versioned kernel state contracts"
      exports: ["GameState", "ActorState", "TeamState", "SceneState", "EncounterState", "ClockState", "ResourceState", "EffectState"]
    - path: "openrpg_cli/domain/messages.py"
      provides: "Closed typed command/event contracts"
    - path: "openrpg_cli/domain/provenance.py"
      provides: "Runtime content provenance and boundary validation"
  key_links:
    - from: "openrpg_cli/domain/state.py"
      to: "openrpg_cli/domain/provenance.py"
      via: "SceneState runtime payload reference"
      pattern: "RuntimeContentRef"
---

<objective>
Define immutable kernel contracts and hard runtime-content boundary.

Purpose: Give later reducers, replay, and gameplay phases stable types without importing application or interface code.
Output: Versioned state graph, closed command/event unions, provenance-safe runtime references, contract tests.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deterministic-gameplay-kernel/05-CONTEXT.md
@openrpg_cli/data.py
@openrpg_cli/character.py
@openrpg_cli/combat.py

<interfaces>
Existing boundaries:
```python
# openrpg_cli/data.py
DATA_ROOT = Path(__file__).resolve().parent / "data" / "srd521"

# openrpg_cli/character.py and openrpg_cli/combat.py use:
@dataclass(frozen=True, slots=True)
class Character: ...
@dataclass(frozen=True, slots=True)
class CombatState: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify immutable state and message contracts</name>
  <files>tests/test_kernel_contracts.py, openrpg_cli/domain/__init__.py, openrpg_cli/domain/state.py, openrpg_cli/domain/messages.py</files>
  <behavior>
    - Every state record is frozen and rejects field mutation.
    - GameState contains schema_version plus tuple/mapping-safe collections for every KERN-01 entity.
    - Each command/event has a unique stable type tag, schema_version, command/event id, aggregate id, and typed payload.
    - Unknown tags cannot be constructed through public union helpers.
  </behavior>
  <action>Write failing contract tests, then create stdlib dataclass/Enum/type-alias contracts. Model game, actor, team, scene, encounter, clock, resource, and effect state explicitly per KERN-01. Store collections as tuples or immutable MappingProxyType-backed values; recursively freeze nested payload values so frozen outer dataclasses cannot hide mutable dict/list state. Define closed tagged command/event variants and public union registries per KERN-02 and D-03. Keep domain package dependency-free: no imports from rules, engine, application, interfaces, CLI, repository, or existing combat implementation. Do not add Phase 6 resolution semantics.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_contracts.py -q</automated>
  </verify>
  <done>All KERN-01 state kinds exist, are recursively immutable/versioned, and command/event tags are unique closed contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce runtime user-content provenance boundary</name>
  <files>tests/test_kernel_contracts.py, openrpg_cli/domain/provenance.py, openrpg_cli/domain/state.py</files>
  <behavior>
    - Runtime content requires provenance kind user-authored plus a non-empty source label.
    - Relative traversal, symlink escape, and paths resolving under openrpg_cli/data/srd521 are rejected.
    - Payload size and recursive depth limits reject hostile input before state creation.
  </behavior>
  <action>Add RuntimeContentRef and validation functions per KERN-05, D-05, D-07. Accept only explicitly configured runtime root; resolve path and require it remain beneath that root while remaining outside DATA_ROOT. Validate bytes/JSON-like payloads with named maximum byte/depth limits. SceneState references runtime content metadata; never embeds data into bundled content root. Keep generic framework provenance-labeled; do not claim runtime data is SRD-authored. Avoid content writes in domain layer.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_kernel_contracts.py -q</automated>
  </verify>
  <done>User-authored runtime data has explicit provenance, bounded shape, safe path containment, and mechanical separation from bundled SRD content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime file/payload → domain state | Untrusted user-authored data enters authoritative contracts |
| domain state → downstream engine | Nested mutability could bypass reducer authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01 | T | state.py | mitigate | Recursively freeze nested values; expose no mutable containers |
| T-05-02 | T/E | provenance.py | mitigate | Resolve paths, reject traversal/symlink escape and bundled-data destinations |
| T-05-03 | D | provenance.py | mitigate | Enforce byte and recursion-depth caps before constructing state |
| T-05-04 | S | provenance.py | mitigate | Require explicit user-authored provenance tag and source label |
</threat_model>

<verification>
`py -3.14 -m pytest tests/test_kernel_contracts.py -q` passes; `py -3.14 -m ruff check openrpg_cli/domain tests/test_kernel_contracts.py` passes.
</verification>

<success_criteria>
State contracts cover every KERN-01 noun; commands/events are closed and versioned; runtime data cannot cross bundled SRD boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/05-deterministic-gameplay-kernel/05-01-SUMMARY.md`
</output>
