---
phase: 07-turn-action-space-engine
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - openrpg_cli/domain/messages.py
  - openrpg_cli/domain/codecs.py
  - openrpg_cli/engine/reducer.py
  - openrpg_cli/interfaces/v1_compat.py
  - tests/test_turn_kernel.py
  - tests/test_turn_v1_compat.py
  - tests/test_release_smoke.py
autonomous: true
requirements: [TURN-01, TURN-02, TURN-03, TURN-04, TURN-05]
must_haves:
  truths:
    - "Turn, action, movement, reaction, and target operations enter kernel as canonical typed commands/events."
    - "Replay reproduces byte-identical N-actor state/events/RNG and invalid commands change nothing."
    - "Existing 1v1 combat CLI/API/results/transcripts and RNG behavior remain compatible."
    - "Audit, full tests, Ruff, build, artifact inspection, and isolated wheel smoke pass."
  artifacts:
    - path: "openrpg_cli/domain/messages.py"
      provides: "Closed Phase 7 command/event types"
    - path: "openrpg_cli/engine/reducer.py"
      provides: "Pure Phase 7 orchestration handlers"
    - path: "tests/test_turn_kernel.py"
      provides: "Canonical replay and hostile-input coverage"
    - path: "tests/test_turn_v1_compat.py"
      provides: "v1 differential compatibility gate"
  key_links:
    - from: "openrpg_cli/engine/reducer.py"
      to: "openrpg_cli/rules/actions.py"
      via: "typed action handler"
      pattern: "resolve_action"
    - from: "openrpg_cli/engine/reducer.py"
      to: "openrpg_cli/rules/turns.py"
      via: "initiative and turn handlers"
      pattern: "TurnState"
    - from: "openrpg_cli/interfaces/v1_compat.py"
      to: "openrpg_cli/combat.py"
      via: "one-way v1 audit adaptation"
      pattern: "adapt"
---

<objective>
Wire Phase 7 into deterministic kernel, prove replay/security, preserve v1 release.

Purpose: Make full turn/action/space engine authoritative without regressing shipped game.
Output: Typed messages, reducer handlers, replay/compat tests, release gates.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-turn-action-space-engine/07-CONTEXT.md
@.planning/phases/05-deterministic-gameplay-kernel/05-04-SUMMARY.md
@.planning/phases/06-universal-resolution-effects/06-04-SUMMARY.md
@openrpg_cli/domain/messages.py
@openrpg_cli/domain/codecs.py
@openrpg_cli/engine/reducer.py
@openrpg_cli/interfaces/v1_compat.py
@openrpg_cli/combat.py
@tests/test_release_smoke.py

<interfaces>
```python
def reduce_command(state: GameState, command: Command, rng: GameRNG) -> ReductionResult: ...
Command: TypeAlias = CreateGame | PutEntity | RollDie | RecordLegacy | ResolveTest | ...
```
Extend closed unions/registries without changing existing type tags/schemas. Store Phase 7 authoritative snapshots in immutable encounter/actor entity data. Rules never import reducer/application/interfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical Phase 7 commands, events, and reducer orchestration</name>
  <files>openrpg_cli/domain/messages.py, openrpg_cli/domain/codecs.py, openrpg_cli/engine/reducer.py, tests/test_turn_kernel.py</files>
  <behavior>
    - Start encounter, query legal commands, act, move, react, and end turn messages round-trip canonical JSON.
    - Reducer reconstructs validated frozen rule state, delegates rules, stores one documented encounter schema, and emits complete transitions.
    - Legal-command query cannot mutate state/RNG; act/move/react spend atomically.
    - Same genesis/command sequence/RNG replays to byte-identical state/event/log hashes in fresh process.
    - Unknown tags/fields, spoofed actor, illegal turn, stale trigger, malformed spatial state, huge collections, and invalid action reject with unchanged state/RNG.
  </behavior>
  <action>Extend typed message unions/registries with narrow Phase 7 commands/events. Use existing EntityState data and canonical `_plain`; do not bump schema unless serialization requires it, then add adjacent migration and migration tests. Per D-01..D-06, event payloads retain costs, targets, range, reasons, rolls/RNG spans, spatial/effect transitions, round/actor. Legal commands are derived server-side from authoritative state, never trusted from payload. Keep reducer orchestration-only and stable event ordering.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_turn_kernel.py tests/test_kernel_codecs.py tests/test_kernel_reducer.py tests/test_kernel_replay.py -q</automated>
  </verify>
  <done>Every Phase 7 operation is canonical, replay-stable, authoritative, and atomically rejects hostile/illegal input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve v1 combat and run full source/release gates</name>
  <files>openrpg_cli/interfaces/v1_compat.py, tests/test_turn_v1_compat.py, tests/test_release_smoke.py</files>
  <behavior>
    - Existing fixed-seed 1v1 results, transcripts, JSON fields/order, errors, CLI output, and RNG draw order remain unchanged.
    - v1 actions adapt one-way into Phase 7 audit records without making v2 depend on mutable v1 combat.
    - N-actor band and equivalent grid examples run from isolated installed wheel and replay identically.
    - Wheel contains only allowlisted SRD 5.2.1 data/license plus code; no runtime scenes, maps, lore, or other content leaks.
  </behavior>
  <action>Per D-06 and hard gate 5, extend one-way v1 compatibility adapter only; do not rewrite `CombatEngine`, alter public signatures, output, errors, initiative, or RNG sequence. Add golden differential fixtures before adapter assertions. Expand isolated wheel smoke for Phase 7 canonical messages/replay. Run audit, complete pytest, Ruff, build, inspect wheel file list/manifest, install to temp target, run outside checkout. Preserve Python 3.11 compatibility and stdlib preference.</action>
  <verify>
    <automated>py -3.14 -m openrpg_cli audit; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest -q; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m ruff check .; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest tests/test_release_smoke.py -q</automated>
  </verify>
  <done>Phase 7 ships replayably while all v1, content-purity, attribution, quality, and isolated-artifact gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| command JSON → reducer | Untrusted actor/action/target/spatial payload enters authoritative state |
| v1 combat → audit adapter | Mutable legacy objects cross into immutable kernel records |
| checkout → wheel | Runtime or prohibited content could enter release artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-17 | S | command actor/trigger | mitigate | Derive ownership/trigger from authoritative state; reject spoof/stale ids |
| T-07-18 | T | codec/reducer | mitigate | Closed tags/fields/enums, bounds, invariant validation, atomic rejection |
| T-07-19 | R | events/log | mitigate | Full command id, actor, costs, targets, SRD keys, RNG spans, before/after hashes |
| T-07-20 | I | artifact | mitigate | Wheel allowlist/manifest/license inspection and isolated audit |
| T-07-21 | D | command collections | mitigate | Caps on actors, targets, areas, paths, triggers, ids, numeric values |
| T-07-22 | E | reducer dependency | mitigate | Tests enforce rules→engine direction and no interface mutation authority |
</threat_model>

<verification>
Phase kernel/replay/compat tests pass. Full audit, pytest, Ruff, build, wheel inspection, isolated install/smoke pass. Git diff confirms no bundled data or v1 output changes.
</verification>

<success_criteria>
TURN-01..05 operate through typed deterministic kernel; range bands/grid share commands; v1 remains byte-compatible; all hard gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-turn-action-space-engine/07-05-SUMMARY.md`
</output>
