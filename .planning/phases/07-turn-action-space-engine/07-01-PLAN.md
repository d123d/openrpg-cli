---
phase: 07-turn-action-space-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/rules/turns.py
  - tests/test_turns.py
autonomous: true
requirements: [TURN-01, TURN-02]
must_haves:
  truths:
    - "Any number of actors across teams receive stable initiative order, round ownership, and surprise state."
    - "Only current actor may spend action, bonus action, movement, or interaction; reactions follow explicit triggers."
    - "Every legal command reports exact cost, target contract, range contract, and rejection reason."
  artifacts:
    - path: "openrpg_cli/rules/turns.py"
      provides: "Immutable initiative, turn, budget, and legal-command contracts"
    - path: "tests/test_turns.py"
      provides: "N-actor, tie-break, surprise, reset, and invalid-spend coverage"
  key_links:
    - from: "openrpg_cli/rules/turns.py"
      to: "openrpg_cli/engine/rng.py"
      via: "explicit initiative RNG"
      pattern: "GameRNG"
---

<objective>
Define deterministic N-actor turn ownership and complete action-economy contracts.

Purpose: Establish authoritative schedule/budget layer consumed by every Phase 7 action.
Output: Pure frozen turn rules plus exhaustive unit tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-turn-action-space-engine/07-CONTEXT.md
@.planning/phases/05-deterministic-gameplay-kernel/05-01-SUMMARY.md
@.planning/phases/06-universal-resolution-effects/06-01-SUMMARY.md
@openrpg_cli/domain/state.py
@openrpg_cli/engine/rng.py
@openrpg_cli/rules/effects.py

<interfaces>
```python
@dataclass(frozen=True, slots=True)
class ActorState(EntityState):
    team_id: str | None = None

@dataclass(frozen=True, slots=True)
class TeamState(EntityState):
    actor_ids: tuple[str, ...] = ()

class GameRNG:
    def die(self, sides: int) -> tuple[int, GameRNG]: ...
```
New contracts remain rules-layer values. No reducer, CLI, mutable v1 combat, or bundled content imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement initiative and legal turn ownership</name>
  <files>openrpg_cli/rules/turns.py, tests/test_turns.py</files>
  <behavior>
    - N actors roll initiative through explicit GameRNG; sort by total, initiative modifier, then stable actor id.
    - Team membership remains explicit; duplicate/missing actor or team refs reject.
    - Surprise is per actor, consumes that actor's first turn restrictions, and never changes initiative order.
    - End-turn advances exactly one eligible actor and increments round only after complete order traversal.
    - Non-current actor commands reject without state or RNG change.
  </behavior>
  <action>Create frozen InitiativeEntry, TurnOrder, TurnState, SurpriseState, and transition/audit records. Stable actor-id tie break is mandatory per D-06. Validate bounded unique ids and team membership at construction. Use explicit GameRNG only; no global random/time. Model surprise according to encoded SRD 5.2.1 semantics with exact SRD key, not legacy surprise-round behavior. Pure functions return new state plus audit facts.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_turns.py -q</automated>
  </verify>
  <done>Arbitrary actor/team sets produce deterministic legal ownership, rounds, surprise transitions, and unchanged inputs on rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement budgets and structured legal-command specs</name>
  <files>openrpg_cli/rules/turns.py, tests/test_turns.py</files>
  <behavior>
    - Turn start deterministically resets one action, one bonus action, movement allowance, and one interaction.
    - Reaction availability resets at actor turn start and may be spent only through an explicit trigger.
    - Costs are atomic; insufficient action, bonus, reaction, movement, or interaction rejects all spending.
    - LegalCommandSpec carries costs, allowed target kinds/count, range contract, prerequisites, and exact failure reason.
  </behavior>
  <action>Per D-01/D-02, define closed BudgetKind/TriggerKind enums, frozen ActionBudget/ActionCost/TargetSpec/RangeSpec/LegalCommandSpec, and pure spend/reset/legality functions. Preserve separate action, bonus, reaction, movement, interaction quantities. Bound movement/costs and command collections to prevent oversized payload work. Do not silently convert budget kinds or let interfaces infer legality.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_turns.py -q</automated>
  </verify>
  <done>Budget resets/spends and legal specs are immutable, explicit, bounded, deterministic, and cover every Phase 7 resource.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| actor/team input → scheduler | Untrusted ids, modifiers, surprise flags enter authoritative order |
| requested cost → budget | Untrusted costs attempt authoritative resource spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-01 | T | turn ownership | mitigate | Closed ids, membership validation, current-actor gate, immutable transitions |
| T-07-02 | R | initiative/budget audit | mitigate | Record rolls, modifiers, tie-break, before/after budgets, SRD keys |
| T-07-03 | D | input collections | mitigate | Bound actor/spec counts, id lengths, modifiers, movement, costs |
| T-07-04 | E | reaction spending | mitigate | Explicit trigger plus owner eligibility; atomic rejection |
</threat_model>

<verification>
Phase tests cover N=1, N>2, teams, exact ties, surprise, round wrap, every budget, malformed ids, bounds, and deterministic reruns.
</verification>

<success_criteria>
TURN-01 and TURN-02 contracts fully work as pure rules; no v1 behavior or content artifact changes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-turn-action-space-engine/07-01-SUMMARY.md`
</output>
