---
phase: 07-turn-action-space-engine
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - srd_cli/rules/tactics.py
  - tests/test_tactics.py
autonomous: true
requirements: [TURN-04]
must_haves:
  truths:
    - "Grapple and shove use explicit opposed/save contracts, size/reach gates, and movement consequences."
    - "Opportunity attacks arise only from eligible movement triggers and spend one reaction."
    - "Cover, visibility, mounted, and underwater modes modify shared action contracts explicitly."
  artifacts:
    - path: "srd_cli/rules/tactics.py"
      provides: "Grapple, shove, reaction, cover, visibility, mount, underwater hooks"
    - path: "tests/test_tactics.py"
      provides: "SRD golden and interaction coverage"
  key_links:
    - from: "srd_cli/rules/tactics.py"
      to: "srd_cli/rules/actions.py"
      via: "attack/action requests"
      pattern: "CoreAction"
    - from: "srd_cli/rules/tactics.py"
      to: "srd_cli/rules/space.py"
      via: "movement and visibility triggers"
      pattern: "SpatialState"
---

<objective>
Add tactical combat hooks without creating alternate turn, target, or resolution engines.

Purpose: Complete Phase 7 combat interactions across movement and environmental modes.
Output: Pure tactical transitions and SRD golden tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-turn-action-space-engine/07-CONTEXT.md
@srd_cli/rules/resolution.py
@srd_cli/rules/effects.py
@srd_cli/rules/turns.py
@srd_cli/rules/space.py
@srd_cli/rules/actions.py

<interfaces>
All tactical rules produce modifiers, legal specs, triggers, or immutable transitions consumed by existing Phase 6/7 resolvers. No second attack/check/budget engine.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement grapple, shove, and opportunity reactions</name>
  <files>srd_cli/rules/tactics.py, tests/test_tactics.py</files>
  <behavior>
    - Grapple and shove validate size, reach, free-hand/target state, action cost, and exact SRD resolution contract.
    - Grapple applies/removes explicit restrained movement relationship; moving grappler handles dragged target cost.
    - Shove chooses prone or push and updates condition/spatial state atomically.
    - Leaving reach emits opportunity trigger only when visible/eligible; Disengage suppresses; accepted attack spends reaction once.
  </behavior>
  <action>Per D-04, create frozen tactical request/result/relationship/trigger contracts with exact SRD keys. Compose `resolve_d20`, effects, action budget, and spatial transitions. Validate all preconditions before any spend. Keep stable trigger ordering for simultaneous movement; actor decides/structured controller input selects reaction, never implicit random choice. Failed grapple/shove still spends its legal action per encoded rule, while malformed commands reject atomically—test distinction.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_tactics.py -q</automated>
  </verify>
  <done>Grapple, shove, drag, and opportunity attacks produce deterministic legal transitions with correct costs and suppression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cover, visibility, mounted, and underwater hooks</name>
  <files>srd_cli/rules/tactics.py, tests/test_tactics.py</files>
  <behavior>
    - Half, three-quarters, and total cover affect target legality/AC/save modifiers exactly; total cover blocks target.
    - Visibility states produce explicit advantage/disadvantage or target rejection and interact with Hide/Search.
    - Mount/dismount, controlled mount occupancy, forced movement, reach, and opportunity triggers use shared space/budget contracts.
    - Underwater weapon/movement/fire constraints emit exact modifiers or unsupported structured reasons.
  </behavior>
  <action>Define closed CoverLevel, VisibilityLevel, EnvironmentMode, MountState and modifier-hook contracts. Apply hooks before shared action resolution; retain sources in audit output. Encode only mechanics directly supported by SRD structured decisions; return explicit unsupported reason for equipment/spell specifics awaiting Phase 9. Avoid maps, vehicle subsystem, swimming creature adapters, or item corpus interpretation.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_tactics.py tests/test_actions.py tests/test_space.py -q</automated>
  </verify>
  <done>Required tactical/environment hooks change common legal/resolution contracts deterministically without hidden inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| movement → reaction queue | Movement may authorize another actor's attack |
| environment/mode → modifiers | Runtime flags alter authoritative resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-13 | S | opportunity trigger | mitigate | Source movement, reactor identity, reach, visibility, hostility validation |
| T-07-14 | T | tactical relationships | mitigate | Immutable ids, invariant checks, atomic multi-state transition |
| T-07-15 | R | modifiers | mitigate | Preserve named source, SRD key, before/after target/cost facts |
| T-07-16 | E | environment hooks | mitigate | Closed modes and explicit unsupported outcomes; no free-form effect execution |
</threat_model>

<verification>
Golden tests cover each tactical feature plus cross-feature cases: disengage/opportunity, hidden/cover, grapple movement, mount reach, underwater attacks, malformed inputs.
</verification>

<success_criteria>
TURN-04 fully works through universal action/space/resolution contracts and remains bounded to Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/07-turn-action-space-engine/07-04-SUMMARY.md`
</output>
