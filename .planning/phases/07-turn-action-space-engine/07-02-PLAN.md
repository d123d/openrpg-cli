---
phase: 07-turn-action-space-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/rules/space.py
  - tests/test_space.py
autonomous: true
requirements: [TURN-05]
must_haves:
  truths:
    - "Range bands are authoritative and resolve movement, distance, areas, and targets without coordinates."
    - "Optional grid positions translate into identical distance and target contracts."
    - "Invalid or hidden/out-of-range targets produce explicit deterministic reasons."
  artifacts:
    - path: "openrpg_cli/rules/space.py"
      provides: "Range-band and grid-neutral spatial contracts"
    - path: "tests/test_space.py"
      provides: "Distance, movement, area, and target parity coverage"
  key_links:
    - from: "openrpg_cli/rules/space.py"
      to: "openrpg_cli/rules/turns.py"
      via: "shared RangeSpec/TargetSpec contracts"
      pattern: "RangeSpec"
---

<objective>
Build coordinate-free spatial authority plus optional grid adapter.

Purpose: Give every action one deterministic distance/area/target language.
Output: Pure spatial model, adapter, movement/target resolvers, parity tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-turn-action-space-engine/07-CONTEXT.md
@openrpg_cli/rules/turns.py
@openrpg_cli/domain/state.py

<interfaces>
Plan 07-01 creates frozen `RangeSpec` and `TargetSpec`; consume directly. Keep range-band result authoritative. Grid is adapter input, never second legality engine.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement range-band movement, areas, and targets</name>
  <files>openrpg_cli/rules/space.py, tests/test_space.py</files>
  <behavior>
    - Same/near/far/remote bands have ordered deterministic distance semantics and bounded movement costs.
    - Moving updates immutable relative positions consistently for every actor pair.
    - Single, multiple, self, ally, enemy, point, line, cone, cube, sphere, and cylinder targets validate count/range.
    - Area resolution returns stable actor-id order and excludes illegal targets with reasons.
  </behavior>
  <action>Per D-05, define closed RangeBand, TargetKind, AreaShape enums and frozen SpatialState/RelativePosition/AreaSpec/TargetResolution/MovementTransition. Encode band thresholds and SRD keys explicitly; keep any abstraction policy clearly engine-authored, not falsely SRD-authored. Validate symmetric relative-position invariants, unique ids, positive bounded dimensions/speeds, and max collection sizes. Pure move/resolve functions return audit transitions.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_space.py -q</automated>
  </verify>
  <done>Coordinate-free play resolves bounded movement, ranges, areas, and targets deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional grid translation with range-band parity</name>
  <files>openrpg_cli/rules/space.py, tests/test_space.py</files>
  <behavior>
    - Integer grid positions translate to range bands and movement costs through one documented metric.
    - Grid and equivalent band inputs produce same target legality and stable target set.
    - Occupancy, blocked movement, invalid coordinates, and oversized paths reject explicitly.
    - Grid state never bypasses authoritative band TargetSpec/RangeSpec evaluation.
  </behavior>
  <action>Per D-05, implement bounded GridPosition/GridState and a one-way adapter to SpatialState/distance contracts. Choose and document square-grid diagonal/distance policy as Codex discretion; test boundary cases. Adapter must normalize first, then call same target resolver. No geometry dependency, floating-point distance, pathfinding subsystem, maps, bundled locations, or content adapters.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_space.py -q</automated>
  </verify>
  <done>Grid use remains optional and yields same action contracts/results as equivalent authoritative bands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime positions → spatial resolver | User-authored coordinates/relationships enter target authority |
| grid adapter → band resolver | Optional representation crosses into canonical rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-05 | T | spatial graph | mitigate | Validate symmetry, unique ids, occupancy, bounds before resolution |
| T-07-06 | R | movement/target result | mitigate | Record source, destination, cost, metric, range, included/excluded targets |
| T-07-07 | D | paths/areas | mitigate | Integer limits and strict caps on path length, dimensions, actor collections |
| T-07-08 | E | grid adapter | mitigate | One-way normalization; single shared legality resolver |
</threat_model>

<verification>
Unit tests cover band boundaries, every target/area shape, movement, grid parity, malformed coordinates, occupancy, stable ordering, and bounds.
</verification>

<success_criteria>
TURN-05 works without coordinates; optional grid produces contract-equivalent results and adds no bundled content.
</success_criteria>

<output>
After completion, create `.planning/phases/07-turn-action-space-engine/07-02-SUMMARY.md`
</output>
