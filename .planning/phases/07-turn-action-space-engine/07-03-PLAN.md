---
phase: 07-turn-action-space-engine
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - openrpg_cli/rules/actions.py
  - tests/test_actions.py
autonomous: true
requirements: [TURN-03]
must_haves:
  truths:
    - "All twelve named core actions expose exact legality, costs, targets, and deterministic transitions."
    - "Attack and Magic delegate resolution rather than inventing content effects."
    - "Ready records a bounded trigger and spends reaction only when trigger later occurs."
  artifacts:
    - path: "openrpg_cli/rules/actions.py"
      provides: "Closed core-action catalog and pure action resolution"
    - path: "tests/test_actions.py"
      provides: "Golden legality/effect coverage for every core action"
  key_links:
    - from: "openrpg_cli/rules/actions.py"
      to: "openrpg_cli/rules/resolution.py"
      via: "attack/check/save resolution"
      pattern: "resolve_d20"
    - from: "openrpg_cli/rules/actions.py"
      to: "openrpg_cli/rules/turns.py"
      via: "atomic budget spend and legal spec"
      pattern: "LegalCommandSpec"
    - from: "openrpg_cli/rules/actions.py"
      to: "openrpg_cli/rules/space.py"
      via: "shared target resolution"
      pattern: "resolve_targets"
---

<objective>
Implement complete SRD core action catalog over shared turn/space/resolution contracts.

Purpose: Make each standard turn choice legally queryable and replay-ready.
Output: Closed action rules and golden behavior tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-turn-action-space-engine/07-CONTEXT.md
@.planning/phases/06-universal-resolution-effects/06-01-SUMMARY.md
@openrpg_cli/rules/resolution.py
@openrpg_cli/rules/effects.py
@openrpg_cli/rules/turns.py
@openrpg_cli/rules/space.py
@openrpg_cli/combat_actions.py

<interfaces>
Use `D20Test`/`resolve_d20`, Phase 7 `LegalCommandSpec`/`ActionBudget`, and spatial target contracts. Do not consume `CombatActionCatalog` as authoritative v2 logic; it remains v1 adapter/content boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define closed action catalog and legal-command generation</name>
  <files>openrpg_cli/rules/actions.py, tests/test_actions.py</files>
  <behavior>
    - Catalog contains exactly Attack, Magic, Dash, Disengage, Dodge, Help, Hide, Influence, Ready, Search, Study, Utilize.
    - Each action emits a structured LegalCommandSpec with action cost, target count/kind, range, prerequisites, and failure reason.
    - Conditions, turn ownership, budget, visibility, and target range filter legal commands deterministically.
    - Unknown action or prose-only effect rejects explicitly.
  </behavior>
  <action>Per D-01/D-03, define CoreAction enum plus frozen action request/outcome contracts and catalog metadata with exact SRD 5.2.1 keys. Generate legal specs from authoritative turn/spatial state. Do not accept display names as ids, infer spell/item/creature effects, or bundle action content. Keep bounded structured parameters. Explicit unsupported reason is required wherever Phase 9 adapter data would be needed.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_actions.py -q</automated>
  </verify>
  <done>Every required core action has exact machine-readable legality and no silent mechanic inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve core action transitions through shared engines</name>
  <files>openrpg_cli/rules/actions.py, tests/test_actions.py</files>
  <behavior>
    - Attack and Magic validate targets/range then delegate typed attack/save/check inputs; supplied structured effects remain outside this catalog.
    - Dash grants deterministic movement budget; Disengage/Dodge/Help/Hide create exact turn-scoped status transitions.
    - Influence/Search/Study resolve correct structured ability tests; Utilize validates interaction/item contract without inventing item effects.
    - Ready records one action plus bounded trigger and later spends reaction atomically when matching trigger occurs.
  </behavior>
  <action>Compose turn budgets, spatial targeting, `resolve_d20`, and Phase 6 effects without duplicate RNG or alternate modifier math. Preserve full audit inputs/results and stable transition ordering. Hide/search visibility changes must be explicit state transitions, not interface state. Ready trigger grammar is closed/bounded. Return rejected outcome with unchanged budget/spatial/RNG for any failure.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_actions.py tests/test_resolution.py tests/test_effects.py -q</automated>
  </verify>
  <done>All twelve actions execute deterministic typed transitions using shared resolution, budget, target, and effect contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| action request → catalog | Untrusted action ids/parameters request authoritative transitions |
| ready trigger → reaction | Deferred user input attempts later execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-09 | S | actor/action request | mitigate | Current-owner identity plus closed action ids |
| T-07-10 | T | action parameters | mitigate | Strict frozen contracts, bounded values, atomic validation before spend |
| T-07-11 | R | action outcome | mitigate | Store costs, targets, SRD keys, RNG spans, transitions, rejection reasons |
| T-07-12 | E | ready/magic/utilize | mitigate | Closed triggers and explicit structured effect boundary; no prose execution |
</threat_model>

<verification>
Parameterized tests cover every action happy path, illegal ownership/budget/target/range, condition restrictions, Ready lifecycle, deterministic RNG, atomic failures.
</verification>

<success_criteria>
TURN-03 fully implemented through shared contracts; no Phase 8/9 progression or corpus adapters added.
</success_criteria>

<output>
After completion, create `.planning/phases/07-turn-action-space-engine/07-03-SUMMARY.md`
</output>
