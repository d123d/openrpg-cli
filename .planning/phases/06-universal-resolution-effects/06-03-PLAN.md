---
phase: 06-universal-resolution-effects
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - openrpg_cli/rules/effects.py
  - tests/test_effects.py
  - tests/test_conditions_srd_golden.py
autonomous: true
requirements: [RES-05]
must_haves:
  truths:
    - "All 15 bundled conditions, including exhaustion, have explicit tested mechanical hooks."
    - "Effect durations advance by deterministic phase/tick without assuming initiative or combat turns."
    - "Unsupported condition prose clauses are named, never silently treated as implemented."
  artifacts:
    - path: "openrpg_cli/rules/effects.py"
      provides: "Condition registry, mechanical hooks, timed effect scheduler"
    - path: "tests/test_effects.py"
      provides: "Scheduler ordering, expiry, stacking, and hook tests"
    - path: "tests/test_conditions_srd_golden.py"
      provides: "One or more SRD-keyed goldens for every bundled condition"
  key_links:
    - from: "openrpg_cli/rules/effects.py"
      to: "openrpg_cli/data/srd521/ConditionDescription.json"
      via: "exact condition-key registry coverage"
      pattern: "srd-2024_"
---

<objective>
Implement complete condition hooks and context-neutral effect scheduler.

Purpose: Make condition/duration semantics deterministic before turn engine exists.
Output: Condition registry, effect contracts, scheduler, coverage tests, SRD goldens.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-resolution-effects/06-CONTEXT.md
@openrpg_cli/domain/state.py
@openrpg_cli/data/srd521/ConditionDescription.json

<interfaces>
Effect scheduler remains independent from Phase 7 initiative/action economy. Callers supply monotonic tick plus closed lifecycle phase; scheduler returns ordered due/expired transitions and updated immutable effects.
</interfaces>
</context>

<feature>
  <name>SRD conditions and deterministic effect lifecycle</name>
  <files>openrpg_cli/rules/effects.py, tests/test_effects.py, tests/test_conditions_srd_golden.py</files>
  <behavior>
    - Registry exactly covers blinded, charmed, deafened, exhaustion, frightened, grappled, incapacitated, invisible, paralyzed, petrified, poisoned, prone, restrained, stunned, unconscious.
    - Each condition exposes typed hooks affecting only mechanics explicitly present in bundled definition: d20 advantage/disadvantage, auto-fail categories, speed/movement flags, action/reaction eligibility flags, attack critical implications, resistance/immunity changes, perception/visibility facts.
    - Exhaustion level is bounded and hook effects derive from level; removal/reduction is explicit.
    - Effects carry stable id, source, target, condition, start tick/phase, optional end tick/phase, stacks policy, and SRD key.
    - Same tick/phase processing is stable by effect id; due callbacks precede expiry per documented contract; repeated advance is idempotent.
    - Permanent/manual, fixed tick, phase-bound, and save-ends/manual clauses remain distinct; scheduler does not invent combat turns or saving opportunities.
  </behavior>
  <implementation>RED: build exact corpus-coverage, hook, stacking, phase-order, expiry, and idempotence tests. GREEN: manually encode registry from all 15 `ConditionDescription.json` records per Phase 6 decision. Include `implemented_hooks` plus `unsupported_clauses` on every definition. Use frozen records, closed enums, bounded ticks/levels. Goldens must load corpus and assert exact key/description mapping for every condition, then test at least one encoded hook or explicit unsupported clause per record. Do not parse prose heuristically, add turn ownership, or put conditions into bundled data.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| effect creation/clock advance → scheduler | User/runtime effect ids, durations, phases, and targets enter authoritative ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-07 | S | effect source | mitigate | Preserve validated source/target ids; never infer authority from labels |
| T-06-08 | T | scheduler | mitigate | Closed phases, monotonic bounded ticks, immutable effects, stable id ordering |
| T-06-09 | R | condition registry | mitigate | Exact SRD keys plus explicit implemented/unsupported clause lists |
| T-06-10 | D | repeat advance | mitigate | Idempotent transition ids and bounded effect collection |
</threat_model>

<verification>
<automated>py -3.14 -m pytest tests/test_effects.py tests/test_conditions_srd_golden.py -q</automated>
</verification>

<success_criteria>
Corpus coverage equals 15/15; each condition has tested hook/unsupported accounting; scheduler is stable, idempotent, combat-neutral.
</success_criteria>

<output>
After completion, create `.planning/phases/06-universal-resolution-effects/06-03-SUMMARY.md`
</output>
