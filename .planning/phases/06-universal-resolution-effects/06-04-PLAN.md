---
phase: 06-universal-resolution-effects
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - srd_cli/domain/messages.py
  - srd_cli/domain/codecs.py
  - srd_cli/engine/reducer.py
  - srd_cli/interfaces/v1_compat.py
  - srd_cli/combat.py
  - tests/test_resolution_kernel.py
  - tests/test_resolution_v1_compat.py
  - tests/test_release_smoke.py
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04, RES-05]
must_haves:
  truths:
    - "Resolution, vitality, and effect operations enter kernel through typed commands/events and replay byte-identically."
    - "Reducer remains pure with explicit RNG and rejected commands leave state/RNG unchanged."
    - "Existing v1 combat public results, JSON, transcript, CLI, and seeded behavior remain compatible."
    - "Audit, full pytest, Ruff, build, and isolated wheel gates pass."
  artifacts:
    - path: "srd_cli/domain/messages.py"
      provides: "Closed gameplay resolution commands/events"
    - path: "srd_cli/engine/reducer.py"
      provides: "Pure handlers delegating to Phase 6 rule functions"
    - path: "tests/test_resolution_kernel.py"
      provides: "Replay and malformed-command coverage"
    - path: "tests/test_resolution_v1_compat.py"
      provides: "v1 differential compatibility coverage"
  key_links:
    - from: "srd_cli/engine/reducer.py"
      to: "srd_cli/rules/resolution.py"
      via: "typed resolve-test handler"
      pattern: "resolve_d20"
    - from: "srd_cli/engine/reducer.py"
      to: "srd_cli/rules/vitality.py"
      via: "typed damage/heal/death handlers"
      pattern: "apply_damage"
    - from: "srd_cli/engine/reducer.py"
      to: "srd_cli/rules/effects.py"
      via: "typed effect apply/advance handlers"
      pattern: "advance"
---

<objective>
Wire Phase 6 rules into deterministic kernel while preserving v1.

Purpose: Make universal resolution replayable without breaking shipped interfaces.
Output: Typed messages/codecs/reducer handlers, v1 adapter bridge, differential tests, release gates.
</objective>

<execution_context>
@C:/AI/claude/get-shit-done/workflows/execute-plan.md
@C:/AI/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-resolution-effects/06-CONTEXT.md
@.planning/phases/05-deterministic-gameplay-kernel/05-04-SUMMARY.md
@.planning/phases/06-universal-resolution-effects/06-01-SUMMARY.md
@.planning/phases/06-universal-resolution-effects/06-02-SUMMARY.md
@.planning/phases/06-universal-resolution-effects/06-03-SUMMARY.md
@srd_cli/domain/messages.py
@srd_cli/domain/codecs.py
@srd_cli/engine/reducer.py
@srd_cli/interfaces/v1_compat.py
@srd_cli/combat.py
@tests/test_combat_engine.py
@tests/test_combat_session.py

<interfaces>
```python
Command: TypeAlias = CreateGame | PutEntity | RollDie | RecordLegacy
Event: TypeAlias = GameCreated | EntityPut | DieRolled | LegacyRecorded | CommandRejected
def reduce_command(state: GameState, command: Command, rng: GameRNG) -> ReductionResult: ...
def adapt_combat_result(result: Any) -> tuple[GameState, GameRNG, SessionLog]: ...
```
Extend closed unions/registries and canonical codec. Preserve existing type tags and decoded schemas. Rule modules never import engine/application/interfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed Phase 6 kernel commands and replay handlers</name>
  <files>srd_cli/domain/messages.py, srd_cli/domain/codecs.py, srd_cli/engine/reducer.py, tests/test_resolution_kernel.py</files>
  <behavior>
    - Resolve-test, apply-damage, heal, apply/remove/advance-effect, death-save, stabilize, and recover commands round-trip canonical JSON.
    - Handlers delegate to rules modules, update immutable actor/effect data, emit complete typed outcome events, and return exact next RNG.
    - Same genesis/commands/RNG replay to byte-identical state, event, and log hashes in fresh interpreters.
    - Unknown fields, invalid SRD keys, malformed state, invalid duration/tick, and oversized collections produce CommandRejected with unchanged state/RNG.
  </behavior>
  <action>Extend closed tagged unions and codec registries without changing existing tags. Add narrow reducer handlers translating validated payloads to/from rule contracts. Store typed vitality/effect snapshots in existing immutable actor/effect entity data with one documented schema; do not mutate nested mappings. Preserve full audit facts and SRD keys in events. Effect advance receives explicit tick/phase. Keep D-01 dependency direction and D-05 scheduler neutrality: domain contracts do not import rule/engine code; reducer imports rules; no CLI mutation.</action>
  <verify>
    <automated>py -3.14 -m pytest tests/test_resolution_kernel.py tests/test_kernel_codecs.py tests/test_kernel_reducer.py tests/test_kernel_replay.py -q</automated>
  </verify>
  <done>All Phase 6 operations are canonical typed messages with pure replay-stable reducer behavior and atomic rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve v1 combat through universal resolvers and release gates</name>
  <files>srd_cli/interfaces/v1_compat.py, srd_cli/combat.py, tests/test_resolution_v1_compat.py, tests/test_release_smoke.py</files>
  <behavior>
    - Existing fixed-seed combat results, transcripts, JSON fields/order, CLI output, and errors remain byte-identical.
    - Legacy attack/save/damage outcomes are differentially compared with Phase 6 resolution records for supported v1 cases.
    - Unsupported legacy shortcuts stay explicitly adapter-labeled rather than changing public behavior or claiming SRD coverage.
    - Installed wheel imports rules and runs representative Phase 6 resolution plus existing v1 smoke outside checkout.
  </behavior>
  <action>Per locked v1 compatibility decision, bridge existing combat attack/save/damage audit records through one-way interface adapter and Phase 6 typed kernel commands. Do not replace v1 turn engine, alter RNG draw order, change combat outcomes, add action economy, or change public signatures/rendering. Differential tests must compare golden existing fixtures before/after and verify adapter replay hashes. Extend release smoke. Run audit, complete suite, Ruff, build, isolated wheel inspection/install/smoke; reject non-SRD/runtime content leakage.</action>
  <verify>
    <automated>py -3.14 -m srd_cli audit; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest -q; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m ruff check .; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m build</automated>
  </verify>
  <done>Universal rules produce replayable v1 audit records while all prior public behavior and source/release hard gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| command JSON → reducer | Untrusted typed gameplay payload enters authoritative state |
| legacy combat → kernel adapter | Mutable v1 values become audit records |
| checkout → wheel | Runtime/user or non-SRD content could leak into artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-11 | T | codecs/reducer | mitigate | Strict fields, closed tags/enums, bounded values, atomic rejection |
| T-06-12 | R | gameplay events | mitigate | Complete SRD key, modifier/defense/lifecycle facts and RNG span |
| T-06-13 | I | wheel | mitigate | Artifact inspection and isolated smoke outside checkout |
| T-06-14 | E | dependency graph | mitigate | Tests enforce domain→rules→engine→application→interfaces direction |
</threat_model>

<verification>
Phase-specific replay/compat tests pass. Full audit, pytest, Ruff, build, isolated wheel smoke pass. Existing v1 output goldens remain unchanged.
</verification>

<success_criteria>
RES-01..05 operate through typed replayable kernel commands; fresh-process hashes match; v1 remains compatible; all hard gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-universal-resolution-effects/06-04-SUMMARY.md`
</output>
