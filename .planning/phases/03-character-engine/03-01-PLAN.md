---
phase: 03-character-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - srd_cli/character.py
  - srd_cli/character_builder.py
  - tests/test_character_builder.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
must_haves:
  truths:
    - "Caller can build a legal level-1 character for every SRD base class, species, background, and origin feat."
    - "Default choices and explicit choices produce identical results across repeated runs."
    - "Invalid content identities fail with readable, deterministic candidates rather than choosing an ambiguous match."
    - "Ability modifiers, proficiency, HP, AC, saves, skills, attacks, and spell DC are derived from legal selections."
  artifacts:
    - path: "srd_cli/character.py"
      provides: "Immutable version-ready character input and derived-stat contracts"
    - path: "srd_cli/character_builder.py"
      provides: "SRD-backed selection, validation, deterministic defaults, and derived-stat engine"
    - path: "tests/test_character_builder.py"
      provides: "Exhaustive legal-choice and stat derivation coverage"
  key_links:
    - from: "srd_cli/character_builder.py"
      to: "srd_cli/api.py"
      via: "RulesAPI exact lookups and aggregate traversal"
      pattern: "RulesAPI|get_rules_api"
    - from: "srd_cli/character_builder.py"
      to: "srd_cli/character.py"
      via: "builder emits immutable Character"
      pattern: "Character\\("
---

<objective>
Define character contracts and deterministic level-1 creation engine over normalized SRD data.

Purpose: Establish single authoritative character state/derivation layer for persistence, sheets, CLI, and later combat.
Output: Immutable contracts, SRD-backed builder, exhaustive unit tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-character-engine/03-CONTEXT.md
@.planning/phases/02-normalized-rules-api/02-04-SUMMARY.md
@srd_cli/api.py
@srd_cli/character_rules.py
@srd_cli/combat_rules.py
@srd_cli/models.py

<interfaces>
From srd_cli/api.py:
```python
class RulesAPI:
    def list_classes(self): ...
    def get_class(self, identity: str): ...
    def list_species(self): ...
    def get_species(self, identity: str): ...
    def list_backgrounds(self): ...
    def get_background(self, identity: str): ...
    def list_feats(self): ...
    def get_feat(self, identity: str): ...
    def list_spells(self): ...
    def get_spell(self, identity: str): ...
    def list_weapons(self): ...
    def get_weapon(self, identity: str): ...
```
From srd_cli/character_rules.py:
```python
@dataclass(frozen=True, slots=True)
class ClassView:
    entity: CharacterClass
    features: tuple[ClassFeatureView, ...]
    spells: tuple[SpellView, ...]
    parent: CharacterClass | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable character contracts</name>
  <files>srd_cli/character.py, tests/test_character_builder.py</files>
  <behavior>
    - Character identity includes name, level=1, class/species/background/feat PKs, six named ability scores, equipment PKs, spell PKs, and explicit user choice records.
    - Derived block contains modifiers, proficiency bonus, max/current HP, AC, save/skill bonuses, attacks, spellcasting ability/attack/DC where applicable.
    - Contracts use frozen dataclasses and immutable tuples/mappings; no source row or RulesAPI aggregate is embedded.
    - Level values other than 1 are rejected without implying progression support.
  </behavior>
  <action>Create typed frozen contracts for `AbilityScores`, creation selections, equipment/spell references, attack summaries, derived stats, and `Character`. Keep persisted identity/reference fields separate from recomputable derived data. Add explicit schema-version constant for downstream serializer. Reject unknown ability keys, booleans masquerading as ints, scores outside 1..30, blank names, and non-level-1 state. This implements level-1 boundary per D-07 while preserving a numeric `level` field for later migration.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_builder.py -x -k contracts</automated>
  </verify>
  <done>Character state has one immutable, typed, level-1 contract suitable for deterministic rebuild and JSON serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build legal SRD selection and deterministic defaults</name>
  <files>srd_cli/character_builder.py, tests/test_character_builder.py</files>
  <behavior>
    - All base SRD classes, all species, four backgrounds, and only feats whose bundled type is Origin are accepted.
    - Default scores use 15,14,13,12,10,8 exactly once, ordered by class primary ability then saving-throw priorities and stable remaining ability order.
    - Explicit scores and legal equipment/spell overrides replace defaults without hidden randomness.
    - Starting equipment and level-1 spells are selected only from joined class/spell records; source ordering is normalized before choosing defaults.
    - Bad or ambiguous values report field name plus stable close-match candidates.
  </behavior>
  <action>Create `CharacterRequest`, structured `ChoiceError`, candidate-suggestion helper, and `CharacterBuilder`. Validate all references through injected `RulesAPI` per D-03. Implement D-01 full base-class/species/background/origin-feat coverage, D-02 standard-array/explicit-score rules, D-06 joined-table equipment/spell sourcing, deterministic default/override behavior. Because current `primary_abilities` source values are empty, encode an exhaustive SRD 5.2.1 base-class primary-ability compatibility map keyed by bundled class PK, fail closed for uncovered base classes, and document this data-adapter boundary; never infer from display text. Filter subclasses via `ClassView.parent`, feats via exact `entity.data["type"] == "Origin"`, spells by class PK and level 0/1. Extract structured starting choices only from joined feature/items; reject overrides not present in legal candidate sets. Use `difflib.get_close_matches` over sorted legal names/PKs; never network-fetch or accept user-authored rules content.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_builder.py -x -k "choices or defaults or suggestions"</automated>
  </verify>
  <done>Every supported SRD selection has deterministic defaults; explicit legal overrides work; illegal/non-SRD input fails with candidates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive complete level-1 statistics</name>
  <files>srd_cli/character_builder.py, tests/test_character_builder.py</files>
  <behavior>
    - Modifier is floor((score-10)/2), proficiency bonus is +2, and level-1 HP is hit-die maximum plus Constitution modifier.
    - AC uses equipped armor base/Dex cap plus shield where legal; unarmored fallback is 10 + Dexterity modifier.
    - Saves and background-granted skills add proficiency exactly once.
    - Weapon attacks expose ability-appropriate attack bonus and damage expression; spellcasters expose attack bonus and DC 8 + proficiency + casting modifier.
    - Same request always yields equal Character and equal ordered derived collections.
  </behavior>
  <action>Implement pure derived-stat functions and have builder assemble final `Character`. Parse only bounded, typed SRD fields/benefits needed for level 1; centralize SRD name-to-ability/skill mappings and fail closed on an unsupported structured value rather than silently dropping it. Resolve equipped weapons/armor through normalized bundled entities; account for finesse/ranged properties using joined weapon properties. Determine casting ability by exhaustive SRD base-class map where source lacks a structured field, returning no spell stats for noncasters. Test representative martial, armored caster, unarmored, finesse/ranged, and full-caster builds plus parameterized all-base-class determinism. Do not implement attacks, rolling, combat, or progression.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_builder.py -x</automated>
  </verify>
  <done>Builder returns playable level-1 character state with deterministic complete derived statistics and no combat behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller request → CharacterBuilder | Untrusted identities, scores, and overrides enter rules engine |
| Bundled normalized data → derivation adapter | Audited content still requires strict structured-field interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | content identities | mitigate | Resolve every identity through RulesAPI; restrict feat/class kinds; fail ambiguity |
| T-03-02 | T | ability/choice input | mitigate | Strict type/range/membership validation before derivation |
| T-03-03 | R | deterministic defaults | mitigate | Stable sorted candidates and equality tests across repeated builds |
| T-03-04 | D | suggestions/input | mitigate | Bound input lengths and suggestion count; no regex or network work |
| T-03-05 | E | unsupported progression | mitigate | Enforce level exactly 1 at contract and builder boundaries |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_character_builder.py`
`py -3.14 -m srd_cli audit`
</verification>

<success_criteria>
- Legal SRD level-1 requests build immutable characters.
- Defaults and derived stats are deterministic.
- Explicit choices are checked against joined SRD candidates.
- Invalid input yields readable stable suggestions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-engine/03-01-SUMMARY.md`
</output>
