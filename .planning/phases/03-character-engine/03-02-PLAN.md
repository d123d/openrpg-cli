---
phase: 03-character-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - srd_cli/character_store.py
  - tests/test_character_store.py
autonomous: true
requirements:
  - CHAR-04
  - CHAR-05
must_haves:
  truths:
    - "Character save/load round-trips without state loss through human-readable versioned JSON."
    - "Load and validate reject tampering, illegal references, malformed JSON, and unsupported schema versions."
    - "Default character storage is isolated under ~/.srd-cli/characters while explicit paths work in temp directories."
  artifacts:
    - path: "srd_cli/character_store.py"
      provides: "Versioned codec, filesystem store, and integrity validator"
    - path: "tests/test_character_store.py"
      provides: "Round-trip, migration boundary, path, and tamper tests"
  key_links:
    - from: "srd_cli/character_store.py"
      to: "srd_cli/character_builder.py"
      via: "rebuild persisted selections against current RulesAPI"
      pattern: "CharacterBuilder"
---

<objective>
Add safe versioned JSON persistence and full character integrity validation.

Purpose: Make character files portable, inspectable, reproducible, and unable to smuggle illegal rules references.
Output: Codec/store/validator plus tamper and round-trip tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-character-engine/03-CONTEXT.md
@.planning/phases/03-character-engine/03-01-SUMMARY.md
@srd_cli/character.py
@srd_cli/character_builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical versioned JSON codec</name>
  <files>srd_cli/character_store.py, tests/test_character_store.py</files>
  <behavior>
    - Encoded JSON has top-level schema_version plus explicit identity, choices, scores, references, and derived block.
    - Output uses stable key ordering, indentation, UTF-8, and terminal newline.
    - Encode/decode/encode is byte-identical for valid current-version files.
    - Missing, extra security-sensitive, wrong-typed, and unsupported-version fields produce path-specific errors.
  </behavior>
  <action>Implement explicit field-by-field codec; do not use `dataclasses.asdict` as implicit schema. Use current schema constant from `character.py`, human-readable names, sorted maps/lists where semantics are unordered, and preserved tuple order where choice order matters per D-04. Decode JSON with strict type guards (including bool-vs-int), duplicate-key detection through `object_pairs_hook`, maximum file-size bound, and clear JSON-path errors. Support only current schema; unsupported versions fail with migration-needed message rather than guessed conversion. Test Unicode names and complete field equality.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_store.py -x -k "codec or schema or round_trip"</automated>
  </verify>
  <done>Current schema round-trips losslessly and canonically; malformed/unknown schemas fail explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe store and integrity rebuild</name>
  <files>srd_cli/character_store.py, tests/test_character_store.py</files>
  <behavior>
    - Save defaults to ~/.srd-cli/characters/{safe-name}.json and accepts explicit file/root paths.
    - Save is atomic and never follows an existing symlink into an unintended target.
    - Load revalidates every reference and recomputes derived values; stored derived tampering is reported.
    - Illegal class/species/background/feat/equipment/spell references and traversal names are rejected.
    - Directory listing returns deterministic metadata for valid files and reports invalid files without crashing.
  </behavior>
  <action>Create `CharacterStore`, `ValidationIssue`, and `validate_character`. Per D-05 use `Path.home()/".srd-cli"/"characters"` only as default; explicit root/file path supports CI. Sanitize generated filenames to a conservative slug, resolve/check containment, reject symlink targets, create parent dirs, write temp file in same dir with `O_EXCL`, flush/fsync, then `os.replace`. On load, decode persisted selections, rebuild through injected `CharacterBuilder` so every reference is checked against RulesAPI per D-03, compare all stored derived fields to recomputed values, and return ordered actionable issues for tampering. Do not mutate or auto-repair files. Bound read size and list only `.json` regular files.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_store.py -x</automated>
  </verify>
  <done>Files save atomically, load losslessly, list deterministically, and expose any schema/rules/derived tampering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Character object → filesystem | User-selected names and paths influence writes |
| JSON file → runtime state | Attacker-controlled bytes and references enter decoder/rules engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | T | character JSON | mitigate | Strict schema plus RulesAPI rebuild and derived comparison |
| T-03-07 | S | content refs | mitigate | Resolve every persisted PK against legal level-1 candidate sets |
| T-03-08 | E | save path | mitigate | Slug containment checks, symlink rejection, explicit regular-file rules |
| T-03-09 | D | JSON load/list | mitigate | File-size cap, duplicate-key rejection, per-file list error isolation |
| T-03-10 | R | writes | mitigate | Canonical bytes and atomic replacement make resulting file reproducible |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_character_store.py`
`py -3.14 -m srd_cli audit`
</verification>

<success_criteria>
- Canonical current-version JSON round-trips without loss.
- Default and explicit storage roots work.
- Unsafe paths/malformed files/illegal refs/tampered stats fail clearly.
- Validation is non-mutating and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-engine/03-02-SUMMARY.md`
</output>
