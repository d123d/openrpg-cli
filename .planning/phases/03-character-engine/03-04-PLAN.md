---
phase: 03-character-engine
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - openrpg_cli/cli.py
  - tests/test_character_cli.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-03
  - CHAR-04
  - CHAR-05
  - CHAR-06
must_haves:
  truths:
    - "User can create characters through prompts or complete non-interactive options."
    - "User can show, list, and validate saved characters through one character command group."
    - "Every command supports CI-safe explicit paths and JSON where applicable."
    - "Failures return nonzero with actionable text and no traceback."
  artifacts:
    - path: "openrpg_cli/cli.py"
      provides: "character create/show/list/validate command group"
    - path: "tests/test_character_cli.py"
      provides: "guided, headless, JSON, persistence, and failure-path CLI coverage"
  key_links:
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/character_builder.py"
      via: "create command request"
      pattern: "CharacterBuilder"
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/character_store.py"
      via: "save/load/list/validate commands"
      pattern: "CharacterStore"
    - from: "openrpg_cli/cli.py"
      to: "openrpg_cli/character_sheet.py"
      via: "show/create output"
      pattern: "render_"
---

<objective>
Expose complete guided and headless character workflow through stable CLI commands.

Purpose: Deliver Phase 3 user outcome by wiring tested engine, persistence, validation, and sheets without duplicating rules logic.
Output: `srd character` command group and end-to-end CLI tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-character-engine/03-CONTEXT.md
@.planning/phases/03-character-engine/03-01-SUMMARY.md
@.planning/phases/03-character-engine/03-02-SUMMARY.md
@.planning/phases/03-character-engine/03-03-SUMMARY.md
@openrpg_cli/cli.py
@openrpg_cli/character_builder.py
@openrpg_cli/character_store.py
@openrpg_cli/character_sheet.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire guided and non-interactive character create</name>
  <files>openrpg_cli/cli.py, tests/test_character_cli.py</files>
  <behavior>
    - `srd character create` prompts for missing name/class/species/background/feat and confirms deterministic defaults.
    - Supplying all required options performs no prompt and works under CliRunner with stdin disabled.
    - Six-score input plus equipment/spell overrides reach builder unchanged.
    - `--output` or `--root` controls save location; `--json` prints machine output.
    - Invalid choice exits nonzero and prints candidates.
  </behavior>
  <action>Add Typer `character` sub-app and `create`. Expose options for name, class, species, background, feat, comma-separated six scores in documented STR,DEX,CON,INT,WIS,CHA order, repeatable equipment/spell overrides, path/root, JSON, and overwrite policy. Per D-01 guided and non-interactive are equal first-class modes: prompt only missing required inputs; use stable numbered legal lists from builder and Typer prompts, never raw free-choice acceptance. Display defaults before save. Convert `ChoiceError`/validation errors to concise stderr and exit 2. Do not duplicate legal-choice or stat logic in CLI.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_cli.py -x -k create</automated>
  </verify>
  <done>Guided and fully headless create paths produce equivalent saved legal characters with readable failure output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire show, list, and validate commands</name>
  <files>openrpg_cli/cli.py, tests/test_character_cli.py</files>
  <behavior>
    - `character show FILE_OR_NAME` loads then prints Rich sheet; `--json` prints valid JSON.
    - `character list` uses default/explicit root and deterministic rows with name/class/species/path/validation state.
    - `character validate FILE_OR_NAME` prints PASS only after schema, references, and derived integrity checks.
    - Missing, malformed, tampered, and illegal files exit nonzero without traceback.
  </behavior>
  <action>Add show/list/validate using only `CharacterStore` and sheet renderers per command ideas. Resolve bare names under selected root and explicit paths directly; never search outside root for bare names. Validate on show as well as explicit validate so illegal files never render as trusted. JSON failure still goes to stderr as concise text and nonzero status. Add temp-root end-to-end tests covering valid workflow, list ordering, tampered derived stat, illegal ref, bad schema, missing file, and output determinism.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_cli.py -x</automated>
  </verify>
  <done>Users can inspect, enumerate, and independently validate character files through predictable CLI contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Run complete Phase 3 regression and purity gates</name>
  <files>tests/test_character_cli.py</files>
  <action>Add subprocess-style smoke assertions for installed entry semantics where CliRunner cannot cover console encoding. Assert legacy info/categories/list/show/search/roll/audit commands still function. Run audit before and after temp-root character operations and assert bundled manifest/data tree remains unchanged, proving character writes never enter package content. Run full test and Ruff gates; fix only Phase 3 regressions in files owned by this plan or coordinate rather than reverting concurrent work.</action>
  <verify>
    <automated>py -3.14 -m openrpg_cli audit &amp;&amp; py -3.14 -m pytest -q &amp;&amp; py -3.14 -m ruff check .</automated>
  </verify>
  <done>All old/new CLI flows, content audit, full tests, and lint pass; runtime character files remain outside bundled SRD data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv/stdin → character services | Untrusted text, paths, scores, and repeatable options enter app |
| Filesystem → CLI output | Malformed/tampered files may be presented as trusted sheets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | S | character show | mitigate | Validate loaded file before rendering trusted sheet |
| T-03-15 | T | create overrides | mitigate | Pass all choices through builder membership validation |
| T-03-16 | E | file/name resolution | mitigate | Bare names resolve only inside selected root; store enforces path safety |
| T-03-17 | I | exceptions | mitigate | Catch domain/parse errors; concise messages without tracebacks/internal paths |
| T-03-18 | D | interactive flow | mitigate | Headless complete-option path never prompts; bounded stable prompt lists |
</threat_model>

<verification>
`py -3.14 -m openrpg_cli audit`
`py -3.14 -m pytest -q`
`py -3.14 -m ruff check .`
</verification>

<success_criteria>
- Guided and non-interactive create both work for legal SRD characters.
- Show/list/validate cover default and explicit storage paths.
- Rich/JSON outputs work; invalid input/file failures are actionable.
- Audit, full tests, and Ruff remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-engine/03-04-SUMMARY.md`
</output>
