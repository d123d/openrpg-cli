---
phase: 03-character-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - openrpg_cli/character_sheet.py
  - tests/test_character_sheet.py
autonomous: true
requirements:
  - CHAR-06
must_haves:
  truths:
    - "User can inspect every creation choice and derived statistic in a readable Rich sheet."
    - "Machine callers can receive complete deterministic JSON representation."
    - "Rendering does not mutate character state or reload content."
  artifacts:
    - path: "openrpg_cli/character_sheet.py"
      provides: "Rich and JSON character renderers"
    - path: "tests/test_character_sheet.py"
      provides: "Complete-field and deterministic-render coverage"
  key_links:
    - from: "openrpg_cli/character_sheet.py"
      to: "openrpg_cli/character.py"
      via: "pure rendering of Character contract"
      pattern: "Character"
---

<objective>
Render complete deterministic character sheets for terminals and machine consumers.

Purpose: Give users inspectable proof of selections, legality-relevant references, and computed play statistics.
Output: Pure Rich/JSON renderers with snapshot-style semantic tests.
</objective>

<execution_context>
@C:/AI/get-shit-done/workflows/execute-plan.md
@C:/AI/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-character-engine/03-CONTEXT.md
@.planning/phases/03-character-engine/03-01-SUMMARY.md
@openrpg_cli/character.py
@openrpg_cli/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render complete Rich character sheet</name>
  <files>openrpg_cli/character_sheet.py, tests/test_character_sheet.py</files>
  <behavior>
    - Sheet shows identity, level, class, species, background, feat, six scores/modifiers, proficiency, HP, AC, saves, skills, equipment, attacks, spells, and spell stats when present.
    - Noncaster and empty collections have clear output without fake spell values.
    - Ordering is stable and terminal output contains no object reprs or raw PKs where display names exist.
    - Narrow/no-color Console capture remains readable.
  </behavior>
  <action>Create pure Rich renderables or `render_sheet(character, console)` using Panel/Table/Columns already installed. Use contract display names captured at creation, group sections predictably, show signed bonuses consistently, and preserve stable attack/spell ordering. D-08 requires Rich sheet; include all derived fields from CHAR-02 and all choices needed to explain character. Avoid terminal-width-dependent omission, global Console dependency, RulesAPI reload, and mutation. Test semantic labels/values through captured no-color output rather than brittle border glyph snapshots.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_sheet.py -x -k rich</automated>
  </verify>
  <done>Rich sheet completely and readably represents martial and caster characters at normal/narrow widths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render deterministic machine JSON</name>
  <files>openrpg_cli/character_sheet.py, tests/test_character_sheet.py</files>
  <behavior>
    - JSON output includes schema version, all identity/choice fields, and every derived field.
    - Repeated rendering is byte-identical.
    - JSON is valid, human-readable, and free of Rich markup.
    - Rendering leaves Character equal to its pre-render value.
  </behavior>
  <action>Add a renderer returning canonical JSON-compatible mapping/text, sharing codec field definitions from `character_store` only if doing so does not create dependency cycle; otherwise expose a neutral serializer helper in `character_sheet`. D-08 requires JSON output in addition to Rich. Ensure arrays/maps have documented deterministic order and add tests asserting complete expected key paths rather than only parse success. No file I/O belongs in renderer.</action>
  <verify>
    <automated>py -3.14 -m pytest -q tests/test_character_sheet.py -x</automated>
  </verify>
  <done>Machine JSON is complete, deterministic, valid, and behaviorally equivalent to Rich sheet data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Character text → terminal/JSON | User-controlled character name reaches output surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | S | Rich markup | mitigate | Render user text as escaped Text, never interpolate markup-enabled strings |
| T-03-12 | I | JSON/sheet | mitigate | Emit only character contract fields; no local paths or internal object reprs |
| T-03-13 | R | output determinism | mitigate | Fixed section/key ordering plus byte-equality tests |
</threat_model>

<verification>
`py -3.14 -m pytest -q tests/test_character_sheet.py`
</verification>

<success_criteria>
- Rich output shows complete legal/derived sheet.
- JSON output contains equivalent complete data.
- Both render paths are deterministic and side-effect free.
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-engine/03-03-SUMMARY.md`
</output>
