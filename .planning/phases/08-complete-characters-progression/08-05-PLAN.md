---
phase: 08-complete-characters-progression
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - openrpg_cli/character.py
  - openrpg_cli/character_builder.py
  - openrpg_cli/character_store.py
  - openrpg_cli/character_sheet.py
  - tests/test_character_schema_v2.py
  - tests/test_character_builder.py
autonomous: true
requirements: [CHAR-20, CHAR-21, CHAR-22, CHAR-23, CHAR-24, CHAR-25]
must_haves:
  truths:
    - "Character schema v2 stores authoritative selections/state and rebuilds every derived projection."
    - "Every valid schema v1 character migrates losslessly and existing v1 create/load/sheet behavior remains compatible."
    - "Tampered derived state, unknown fields, future versions, and invalid cross-references reject."
  artifacts:
    - path: "openrpg_cli/character.py"
      provides: "Character schema v2 aggregate"
    - path: "openrpg_cli/character_store.py"
      provides: "Strict adjacent v1→v2 migration and canonical codec"
    - path: "tests/test_character_schema_v2.py"
      provides: "Migration, round-trip, tamper, and compatibility gates"
  key_links:
    - from: "openrpg_cli/character_builder.py"
      to: "openrpg_cli/rules/grants.py"
      via: "authoritative choice resolution"
      pattern: "resolve"
---
<objective>Integrate Phase 8 rules into lossless character schema v2 while preserving shipped v1 UX.</objective>
<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/08-complete-characters-progression/08-CONTEXT.md
@openrpg_cli/character.py
@openrpg_cli/character_builder.py
@openrpg_cli/character_store.py
@openrpg_cli/character_sheet.py
@openrpg_cli/rules/progression.py
@openrpg_cli/rules/grants.py
@openrpg_cli/rules/inventory.py
@openrpg_cli/rules/spellcasting.py
@openrpg_cli/rules/rests.py
<interfaces>
```python
CHARACTER_SCHEMA_VERSION = 1
class CharacterCodec:
    def encode(self, character: Character) -> bytes: ...
    def decode(self, payload: bytes) -> Character: ...
```
Replace character schema authority only through adjacent migration; do not alter kernel GameState schema in this plan.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define schema v2 aggregate and rebuild derived projections</name>
  <files>openrpg_cli/character.py, openrpg_cli/character_builder.py, openrpg_cli/character_sheet.py, tests/test_character_builder.py, tests/test_character_schema_v2.py</files>
  <behavior>
    - Aggregate stores identity, ability inputs, exact source selections, class levels/subclasses, resolved choices, inventory, spell selections, spendable resources, hit dice, and active concentration refs.
    - Derived stats/proficiencies/features/slots/encumbrance/equipment summaries rebuild from authoritative fields.
    - Builder validates whole aggregate and rejects forged derived or cross-domain inconsistent state.
    - Existing level-1 CharacterRequest yields equivalent v1-visible sheet fields/output.
  </behavior>
  <action>Per D-01..D-06, compose pure Phase 8 modules into immutable schema v2. Keep exact SRD keys internally and stable presentation labels. Do not duplicate rule tables in builder. Maintain CharacterRequest as compatibility adapter producing schema v2, preserving its public args/errors and existing sheet fields.</action>
  <verify><automated>py -3.14 -m pytest tests/test_character_builder.py tests/test_character_sheet.py tests/test_character_schema_v2.py -q</automated></verify>
  <done>One authoritative aggregate supports every Phase 8 subsystem while legacy level-1 output remains compatible.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add strict lossless v1→v2 migration and canonical persistence</name>
  <files>openrpg_cli/character_store.py, tests/test_character_store.py, tests/test_character_schema_v2.py</files>
  <behavior>
    - Every valid v1 fixture migrates to v2, preserves all source choices/identity/abilities and equivalent visible derived values, then canonical round-trips.
    - Migration is adjacent, deterministic, idempotent at target, never downgrades, never guesses ambiguous data silently.
    - Duplicate keys, unknown fields, oversized/deep collections, future versions, invalid refs, and tampered derived values reject.
    - Existing save paths, traversal/symlink protection, list/load behavior remain unchanged.
  </behavior>
  <action>Increment CHARACTER_SCHEMA_VERSION to 2. Implement explicit `_migrate_v1_to_v2` before strict v2 decoding; retain original values in traceable authoritative fields. If legacy data cannot establish a new optional subsystem, use empty valid state with migration provenance—not invented grants. Recompute then compare legacy visible derived values; fail with path-specific error on contradiction.</action>
  <verify><automated>py -3.14 -m pytest tests/test_character_store.py tests/test_character_cli.py tests/test_character_schema_v2.py -q</automated></verify>
  <done>All v1 saves load losslessly as v2; v2 is canonical, strict, bounded, tamper-evident.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| save JSON → character aggregate | Untrusted persisted fields and old schema cross into authority |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-17 | T | codec/migration | mitigate | Duplicate/unknown rejection, adjacent allowlist, full invariant rebuild |
| T-08-18 | E | derived fields | mitigate | Derived projections never authoritative; reject mismatch |
| T-08-19 | D | save payload | mitigate | Existing byte/depth plus per-collection/numeric bounds |
| T-08-20 | I | errors | mitigate | Path-specific errors without dumping full save/content |
</threat_model>
<verification>Golden v1 corpus migrates; v2 canonical round-trip/tamper/future-version/hostile payload cases pass.</verification>
<success_criteria>Schema v2 preserves v1 losslessly and exposes complete Phase 8 authoritative state.</success_criteria>
<output>After completion, create `.planning/phases/08-complete-characters-progression/08-05-SUMMARY.md`</output>
