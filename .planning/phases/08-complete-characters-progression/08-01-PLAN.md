---
phase: 08-complete-characters-progression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - srd_cli/rules/progression.py
  - srd_cli/rules/character_coverage.py
  - tests/test_progression.py
  - tests/test_character_coverage.py
autonomous: true
requirements: [CHAR-20, CHAR-21]
must_haves:
  truths:
    - "Every SRD base class has a deterministic level 1-20 progression assembled from ClassFeature and ClassFeatureItem joins."
    - "Legal subclass selection and multiclass prerequisites are validated from authoritative selections."
    - "Every feature mechanic is classified as structured or explicitly unsupported; prose is never silently executed."
  artifacts:
    - path: "srd_cli/rules/progression.py"
      provides: "Immutable class, level, subclass, XP, and multiclass contracts"
    - path: "srd_cli/rules/character_coverage.py"
      provides: "Complete feature support registry"
    - path: "tests/test_character_coverage.py"
      provides: "All-class/all-level corpus coverage gate"
  key_links:
    - from: "srd_cli/rules/progression.py"
      to: "ClassFeatureItem.json"
      via: "feature parent and level join"
      pattern: "ClassFeatureItem"
---

<objective>
Create complete authoritative progression catalog and honest feature-mechanics registry.

Purpose: Give every SRD class/subclass a deterministic 1-20 structural backbone without prose inference.
Output: Pure progression rules, corpus coverage registry, exhaustive tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-complete-characters-progression/08-CONTEXT.md
@srd_cli/normalized.py
@srd_cli/models.py
@srd_cli/rules/resolution.py

<interfaces>
```python
class NormalizedRepository:
    def load(self, filename: str) -> tuple[Entity, ...]: ...
    def parent_index(self, filename: str) -> Mapping[str, tuple[Relationship, ...]]: ...
class CharacterClass(Entity): ...
class ClassFeature(Relationship): ...
class ClassFeatureItem(Relationship): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build immutable all-class progression and advancement contracts</name>
  <files>srd_cli/rules/progression.py, tests/test_progression.py</files>
  <behavior>
    - Base classes are CharacterClass rows with no subclass_of; subclasses join to their base class.
    - ClassFeatureItem parent joins determine feature availability at each class level 1-20; ordering is level then stable SRD key.
    - XP advancement validates monotonic level transitions and supports milestone input explicitly without inventing XP awards.
    - Multiclass additions validate SRD prerequisites, class level totals, subclass ownership, proficiency bonus by total level, and no duplicate subclass.
    - Invalid class, level 0/21, skipped level, malformed join, or unmet prerequisite rejects without partial output.
  </behavior>
  <action>Implement frozen pure contracts and builders over normalized SRD rows. Per D-01, derived progression is rebuilt from authoritative class-level/subclass selections. Per D-03, generate full tables by joining CharacterClass → ClassFeature → ClassFeatureItem, never regex-infer execution from prose. Encode XP thresholds and multiclass prerequisites only from explicit SRD 5.2.1 rules/keys, retaining source keys in outputs. Cover all base classes and their bundled subclasses at every level.</action>
  <verify><automated>py -3.14 -m pytest tests/test_progression.py -q</automated></verify>
  <done>All SRD base classes produce complete stable level 1-20 feature/resource availability; advancement and multiclass legality are deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require explicit support disposition for every feature mechanic</name>
  <files>srd_cli/rules/character_coverage.py, tests/test_character_coverage.py</files>
  <behavior>
    - Every ClassFeature, SpeciesTrait, BackgroundBenefit, and FeatBenefit key appears exactly once.
    - Registry entries use closed structured, structural-only, or unsupported dispositions with nonempty reason and source key.
    - Unknown/duplicate/stale entries fail coverage generation.
    - Structural grants remain available even when procedural mechanic is unsupported.
  </behavior>
  <action>Per D-02/D-04 and hard gate 3, create explicit machine-readable registry. Separate structural availability from executable mechanics. Do not mark prose mechanics supported unless backed by typed descriptor/resolver test. Emit deterministic coverage rows suitable for Phase 9 extension; do not implement Phase 9 spell/monster effects.</action>
  <verify><automated>py -3.14 -m pytest tests/test_character_coverage.py -q</automated></verify>
  <done>Corpus has zero silent feature gaps and unsupported reasons are explicit, stable, auditable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| bundled JSON → progression compiler | Corpus relations and prose enter executable catalog |
| character selections → advancement | Untrusted class/subclass/level choices request grants |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-01 | T | corpus joins | mitigate | Validate keys, parent ownership, levels, duplicates, stable ordering |
| T-08-02 | E | feature registry | mitigate | Closed dispositions; unsupported prose cannot become executable |
| T-08-03 | R | progression output | mitigate | Retain SRD keys and selection provenance |
| T-08-04 | D | selection input | mitigate | Bound levels, class count, ids, and collection sizes |
</threat_model>

<verification>Progression and coverage tests enumerate every base class, subclass, level, and grant record. No foreign content or prose execution.</verification>
<success_criteria>CHAR-20 structural progression and CHAR-21 feature availability complete for every SRD class level 1-20.</success_criteria>
<output>After completion, create `.planning/phases/08-complete-characters-progression/08-01-SUMMARY.md`</output>
