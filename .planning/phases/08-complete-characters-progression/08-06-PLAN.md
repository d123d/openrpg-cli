---
phase: 08-complete-characters-progression
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - srd_cli/domain/messages.py
  - srd_cli/domain/codecs.py
  - srd_cli/engine/reducer.py
  - srd_cli/interfaces/v1_compat.py
  - tests/test_character_kernel.py
  - tests/test_character_v1_compat.py
  - tests/test_release_smoke.py
autonomous: true
requirements: [CHAR-20, CHAR-21, CHAR-22, CHAR-23, CHAR-24, CHAR-25]
must_haves:
  truths:
    - "Advance, choose, inventory, equip, prepare, spend, concentrate, and rest operations use canonical typed kernel commands/events."
    - "Invalid character commands reject atomically; replay produces byte-identical state/events/RNG."
    - "v1 CLI/API/combat/character behavior and all content/release gates remain green."
  artifacts:
    - path: "srd_cli/domain/messages.py"
      provides: "Closed Phase 8 commands/events"
    - path: "srd_cli/engine/reducer.py"
      provides: "Authoritative Phase 8 orchestration"
    - path: "tests/test_character_kernel.py"
      provides: "Replay and hostile-input gate"
  key_links:
    - from: "srd_cli/engine/reducer.py"
      to: "srd_cli/rules/rests.py"
      via: "typed orchestration only"
      pattern: "rest"
---
<objective>Wire complete characters into kernel; prove replay, compatibility, security, release purity.</objective>
<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/phases/08-complete-characters-progression/08-CONTEXT.md
@.planning/phases/07-turn-action-space-engine/07-05-SUMMARY.md
@srd_cli/domain/messages.py
@srd_cli/domain/codecs.py
@srd_cli/engine/reducer.py
@srd_cli/interfaces/v1_compat.py
@tests/test_release_smoke.py
<interfaces>
```python
def reduce_command(state: GameState, command: Command, rng: GameRNG) -> ReductionResult: ...
```
Phase 8 character snapshots live in immutable ActorState data. Character schema v2 is distinct from kernel SCHEMA_VERSION; bump kernel schema only if serialization shape itself changes.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add canonical character commands/events and reducer handlers</name>
  <files>srd_cli/domain/messages.py, srd_cli/domain/codecs.py, srd_cli/engine/reducer.py, tests/test_character_kernel.py</files>
  <behavior>
    - Advancement/choice/inventory/equipment/spell/resource/concentration/rest messages strict-round-trip canonical JSON.
    - Reducer loads authoritative character state, delegates pure rule fn, writes validated snapshot, emits complete source-key/delta event.
    - Same command sequence/RNG replays byte-identically; non-RNG commands preserve RNG.
    - Spoofed actor/source grant, stale choice, forged item, over-spend, illegal spell, duplicate rest, malformed/huge payload rejects with unchanged state/RNG.
  </behavior>
  <action>Extend closed unions/registries with narrow operation commands/events. Derive actor ownership and legal options server-side. Reducer stays orchestration-only; no prose interpretation. Use stable ordering and complete audit facts. Keep existing type tags and behavior unchanged. Add bounds at codec/rule entry.</action>
  <verify><automated>py -3.14 -m pytest tests/test_character_kernel.py tests/test_kernel_codecs.py tests/test_kernel_reducer.py tests/test_kernel_replay.py -q</automated></verify>
  <done>All Phase 8 mutations are typed, authoritative, atomic, bounded, replay-stable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Preserve v1 and execute full source/artifact gates</name>
  <files>srd_cli/interfaces/v1_compat.py, tests/test_character_v1_compat.py, tests/test_release_smoke.py</files>
  <behavior>
    - Existing character create/save/load/list/sheet CLI and API visible behavior remains compatible while stored schema migrates.
    - Existing fixed-seed combat results/transcripts/RNG order remain unchanged.
    - Installed wheel can migrate v1, build every base class at levels 1 and 20, execute inventory/cast/rest/replay smoke.
    - Wheel contains only code, allowlisted SRD 5.2.1 data, license/attribution; unsupported registry has complete corpus coverage.
  </behavior>
  <action>Extend one-way compatibility adapters only; never make v2 depend on mutable v1 objects. Run audit, full pytest, Ruff, build, wheel file/manifest inspection, isolated install outside checkout, Python 3.11-compatible smoke. Verify unsupported registry count/keys against bundled data. Review diff for prohibited content and unchanged v1 public outputs.</action>
  <verify><automated>py -3.14 -m srd_cli audit; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest -q; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m ruff check .; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; py -3.14 -m pytest tests/test_release_smoke.py -q</automated></verify>
  <done>Phase 8 ships complete/replayable with v1 compatibility, SRD purity, attribution, tests, lint, build, isolated artifact green.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| command JSON → reducer | Untrusted actor/choice/item/spell/rest payload enters authority |
| v1 save/API → v2 | Legacy mutable representation crosses compatibility boundary |
| checkout → wheel | Prohibited or runtime content could enter artifact |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-21 | S | actor/grant ownership | mitigate | Derive ownership and legal sources from state |
| T-08-22 | T | reducer | mitigate | Strict closed messages, invariant rebuild, atomic rejection |
| T-08-23 | R | events/log | mitigate | Command/source keys, deltas, RNG spans, hashes |
| T-08-24 | I | wheel | mitigate | File allowlist, manifest/license inspection, isolated audit |
| T-08-25 | D | payloads | mitigate | Byte/depth/count/id/numeric caps |
| T-08-26 | E | interfaces | mitigate | Reducer sole authority; adapters one-way; rules cannot import engine |
</threat_model>
<verification>Kernel/replay/compat tests plus audit, complete pytest, Ruff, build, wheel inspection, isolated smoke pass.</verification>
<success_criteria>CHAR-20..25 operate through deterministic kernel; every class 1-20 covered; v1 and hard gates preserved.</success_criteria>
<output>After completion, create `.planning/phases/08-complete-characters-progression/08-06-SUMMARY.md`</output>
