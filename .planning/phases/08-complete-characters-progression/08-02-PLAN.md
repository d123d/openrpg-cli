---
phase: 08-complete-characters-progression
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - srd_cli/rules/grants.py
  - tests/test_grants.py
autonomous: true
requirements: [CHAR-21, CHAR-22]
must_haves:
  truths:
    - "Species, background, feat, class, subclass, and multiclass grants resolve through one typed choice graph."
    - "Skills, saves, weapons, armor, tools, languages, proficiency, and expertise are projected without duplicates or hidden defaults."
    - "Missing, excess, ineligible, or conflicting choices return exact validation paths."
  artifacts:
    - path: "srd_cli/rules/grants.py"
      provides: "Typed grant descriptors, choice graph, and proficiency projection"
    - path: "tests/test_grants.py"
      provides: "SRD grant and hostile-choice coverage"
  key_links:
    - from: "srd_cli/rules/grants.py"
      to: "srd_cli/rules/progression.py"
      via: "feature availability input"
      pattern: "Progression"
---

<objective>
Resolve all character-origin and progression grants through validated typed choices.

Purpose: Make proficiencies, expertise, languages, tools, saves, skills, and feature choices complete and reproducible.
Output: Pure grant/choice engine plus exhaustive tests.
</objective>

<execution_context>
@C:/Users/dytho/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/dytho/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/08-complete-characters-progression/08-CONTEXT.md
@srd_cli/character.py
@srd_cli/character_builder.py
@srd_cli/rules/progression.py
@srd_cli/rules/character_coverage.py
@srd_cli/data/srd521/BackgroundBenefit.json
<interfaces>
```python
@dataclass(frozen=True, slots=True)
class ChoiceRecord:
    category: str
    value: str
```
Use exact SRD keys internally; display names never establish identity.
</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define typed grant descriptors and complete choice graph</name>
  <files>srd_cli/rules/grants.py, tests/test_grants.py</files>
  <behavior>
    - Grant sources cover species traits, background benefits, feats, class/core traits, subclass features, multiclass grants, and level features.
    - Choice nodes declare stable id, source key, kind, cardinality, eligible exact keys, prerequisites, and dependencies.
    - Resolver rejects missing/excess/duplicate/ineligible/cyclic choices with source path and does not infer prose choices.
    - Choice order does not affect canonical projection.
  </behavior>
  <action>Per D-02, model typed effect/choice descriptors and DAG validation. Use explicit adapters for structured corpus shapes plus registry disposition from 08-01; unsupported procedural clauses stay visible but do not block structural grant nodes. Preserve legacy CharacterRequest behavior as downstream adapter concern, not alternate authority.</action>
  <verify><automated>py -3.14 -m pytest tests/test_grants.py -q</automated></verify>
  <done>Every structurally available grant has deterministic source-traceable resolution or explicit unsupported disposition.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Project complete proficiency, expertise, language, tool, save, and skill state</name>
  <files>srd_cli/rules/grants.py, tests/test_grants.py</files>
  <behavior>
    - Proficiency kinds remain distinct; expertise requires underlying proficiency and never stacks twice.
    - Class, multiclass, species, background, and feat sources union deterministically while retaining provenance.
    - Proficiency bonus derives from total character level; saves and skills expose exact ability associations.
    - Invalid language/tool/skill keys and forged grant sources reject.
  </behavior>
  <action>Implement immutable projection from resolved choices. Per D-01, never persist derived bonuses as authority. Use exact normalized SRD keys; keep human labels presentation-only. Include all classes and representative overlapping-grant/multiclass golden cases.</action>
  <verify><automated>py -3.14 -m pytest tests/test_grants.py -q</automated></verify>
  <done>CHAR-22 state is complete, source-auditable, canonical, and rebuilt from selections.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user choices → grant resolver | Untrusted keys/cardinality/dependencies request capabilities |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-05 | S | grant source | mitigate | Source keys derived from authoritative graph, never accepted from client |
| T-08-06 | T | choice DAG | mitigate | Closed kinds, exact keys, cycle/cardinality/prerequisite validation |
| T-08-07 | E | expertise/proficiencies | mitigate | Rebuild union from validated grants; reject forged capabilities |
| T-08-08 | D | choice graph | mitigate | Bound nodes/options/depth and reject cycles |
</threat_model>
<verification>Grant tests cover every source family, class, overlap, expertise rule, malformed graph, and canonical ordering.</verification>
<success_criteria>CHAR-21/22 grants and proficiencies work for all legal character paths without silent inference.</success_criteria>
<output>After completion, create `.planning/phases/08-complete-characters-progression/08-02-SUMMARY.md`</output>
